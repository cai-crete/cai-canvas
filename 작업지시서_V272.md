# 작업지시서 V272 (최종판)

**작성일**: 2026-03-30
**버전**: V272
**기반 버전**: V271
**상태**: 승인 완료

---

## 개요

7개 변경:

| # | 내용 | 난이도 |
|---|------|--------|
| A | 아이템 클릭 시 패널 자동 오픈 해제 | 低 |
| B | 제어바 edit 버튼 → '+' 아이콘 변경 | 低 |
| C | CHANGE VIEWPOINT 트리거 재설계 (drag&drop 폐기 → ANALYZE 버튼) | 高 |
| D | 제어바 버튼 재구성 (upload/generated/sketch_generated 각각) | 中 |
| E | VIEWPOINT 이미지 edit → 연결 해제 + 모체화 | 中 |
| F | 아이템 하단 텍스트 레이블 추가 | 中 |
| G | edit 탭 시스템 (기존 이미지 위치에 버전 탭으로 생성) | 高 |

---

## Q&A 확정

| 질문 | 답변 |
|------|------|
| Q1. ANALYZE 이미지 재생성 모델 | 이미지 재생성(gemini-flash-image) → gemini-3.1-pro-preview (ANALYSIS 상수) 변경 |
| Q2. library 버튼 기능 | 방안 3 — 플레이스홀더 (클릭 시 "준비 중" 알림) |
| Q3/Q7. [<][>] 버튼 역할 | 아카이빙 확인용 (navigation only). 새 버전은 edit(+) 클릭마다 생성 |
| Q4. GENERATE 조건 | analyzedOpticalParams 있을 때만 표시 |
| Q5. 기존 방식 유지 | — |
| Q6. 탭 방안 | 방안 A — editVersions 배열을 원본 아이템 내부에 저장 |

---

## 변경 상세

### Change A: 아이템 클릭 → 패널 자동 오픈 해제 (line ~558~570)

`setIsRightPanelOpen(true)` 2곳 제거. `setSelectedFunction` 유지.

---

### Change B: edit → Plus 아이콘

제어바의 edit 버튼 (Pencil → `<Plus>` 아이콘).

---

### Change C: CHANGE VIEWPOINT 트리거 재설계

#### C-1: SitePlanDiagram drag&drop / upload 제거

- `fileInputRef`, `handleFileChange`, `isDragOver`, `handleDrop` 제거
- `onDragOver` / `onDragLeave` / `onDrop` 핸들러 제거
- 오버레이 클릭 업로드 + `<input type="file">` 제거
- `onImageDrop` prop 제거

#### C-2: ANALYZE 버튼 (GENERATE 버튼과 조건부 표시)

- `!analyzedOpticalParams` → ANALYZE 버튼 표시
- `analyzedOpticalParams` 있음 → GENERATE 버튼 표시

#### C-3: handleAnalyze 함수

```ts
const handleAnalyze = async () => {
  const sourceItem = canvasItems.find(i => i.id === selectedItemIds[0]);
  if (!sourceItem?.src) return;

  setIsAnalyzing(true);
  setAnalysisStep('이미지 재생성 중...');
  let analysisImage = sourceItem.src;

  try {
    // 이미지 재생성 — gemini-3.1-pro-preview (ANALYSIS)
    const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });
    const base64Data = sourceItem.src.split(',')[1];
    const mimeType = sourceItem.src.split(';')[0].split(':')[1];
    const regenResult = await ai.models.generateContent({
      model: ANALYSIS,
      contents: { parts: [
        { inlineData: { data: base64Data, mimeType } },
        { text: '...' }
      ]}
    });
    for (const part of regenResult.candidates?.[0]?.content?.parts || []) {
      if (part.inlineData) {
        analysisImage = `data:${part.inlineData.mimeType};base64,${part.inlineData.data}`;
        break;
      }
    }
  } catch (e) {
    console.warn('Regen step failed, using original', e);
  } finally {
    setIsAnalyzing(false);
    setAnalysisStep('');
  }

  setSitePlanImage(analysisImage);
  setCanvasItems(prev => prev.map(i =>
    i.id === sourceItem.id ? { ...i, label: 'ANALYZED' } : i
  ));
  await analyzeViewpoint(analysisImage, sourceItem.id);
};
```

---

### Change D: 제어바 버튼 재구성

| 타입 | 버튼 구성 |
|------|-----------|
| upload | [edit(+)] \| [download] \| [library*] \| [delete] |
| generated | [edit(+)] \| [download] \| [delete] |
| sketch_generated | [edit(+)] \| [download] \| [delete] |
| artboard | 기존 유지: [Upload] \| [Lock] \| [Delete] |

*library: 방안 3 플레이스홀더

---

### Change E: VIEWPOINT generated → 연결 해제 + 모체화

`generated` 아이템 edit(+) 클릭 시:
```ts
{ ...i, motherId: null, type: 'upload', editVersions: [{ src: i.src!, label: i.label || 'VIEWPOINT' }], activeVersionIndex: 0 }
```

---

### Change F: 아이템 하단 텍스트 레이블

#### F-1: CanvasItem 타입 확장
```ts
label?: string;
editVersions?: Array<{ src: string; label: string }>;
activeVersionIndex?: number;
```

#### F-2: 레이블 설정 시점

| 시점 | 아이템 | 레이블 |
|------|--------|--------|
| SKETCH TO IMAGE GENERATE | 소스 artboard | `'SKETCH'` |
| SKETCH TO IMAGE GENERATE | 신규 sketch_generated | `'GENERATED'` |
| CV ANALYZE 완료 | upload 원본 | `'ANALYZED'` |
| CV GENERATE 완료 | 신규 generated | `'VIEWPOINT 01~'` |

VIEWPOINT 카운터: `canvasItems.filter(i => i.label?.startsWith('VIEWPOINT')).length + 1`

#### F-3: 렌더링 (아이템 내부 하단 오버레이)

editVersions 없을 때만 표시:
```tsx
{!item.editVersions && item.label && (
  <div className="absolute bottom-0 left-0 right-0 flex items-center justify-center py-1 pointer-events-none">
    <span className="font-mono text-[10px] uppercase tracking-widest opacity-60">{item.label}</span>
  </div>
)}
```

---

### Change G: edit 탭 시스템 (방안 A)

#### G-1: CanvasItem 타입 확장 (F-1에 포함)

#### G-2: displaySrc (아이템 렌더링 내부)
```ts
const displaySrc = item.editVersions?.length
  ? (item.editVersions[item.activeVersionIndex ?? 0]?.src ?? item.src)
  : item.src;
```

#### G-3: edit(+) 클릭 시 editVersions 초기화 + 패널 오픈
- 이미 editVersions 있으면 유지, 없으면 `[{ src: item.src!, label: item.label || 'ORIGINAL' }]` 초기화
- `setSelectedFunction('SKETCH TO IMAGE')`, `setIsRightPanelOpen(true)`

#### G-4: 탭 네비게이션 UI (아이템 내부 하단)
- `editVersions` 있을 때만 표시
- [<] 이전 버전 (index===0 시 disabled)
- 현재 버전 레이블 표시
- [>] 다음 버전 (마지막 버전 시 disabled)

#### G-5: SKETCH TO IMAGE GENERATE → editVersions 분기
소스 아이템에 `editVersions` 있으면 새 캔버스 아이템 생성 대신 버전 추가:
```ts
label: `EDIT ${String(editVersions.length).padStart(2, '0')}`
activeVersionIndex: editVersions.length  (새 버전 인덱스)
```

---

## 적용 파일

`src/App.tsx` 단독 수정

---

## 작업 순서

1. Change A — setIsRightPanelOpen(true) 2곳 제거
2. F-1/G-1 — CanvasItem 타입 확장
3. C-1 — SitePlanDiagram drag&drop 제거
4. C-3 — handleAnalyze + navigateVersion 함수 추가
5. Change B + D + E — 제어바 재구성 (Plus 아이콘, 버튼 재배치)
6. Change C-2 — ANALYZE/GENERATE 조건부 표시
7. F-2 — GENERATE 완료 시 label 설정
8. F-3/G-4 — 아이템 내부 label 오버레이 + 탭 UI
9. G-2/G-3/G-5 — displaySrc + editVersions 로직

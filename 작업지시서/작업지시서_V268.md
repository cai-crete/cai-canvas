# 작업지시서 V268 (갱신판)

**작성일**: 2026-03-29
**버전**: V268
**기반 버전**: V267
**상태**: 완료

---

## 개요

5개 변경 그룹:

| # | 내용 | 난이도 |
|---|---|---|
| A | artboard/sketch_generated 삭제 시 자식 path cascade | 低 |
| B | pen/eraser 모드에서 아트보드 밖 클릭 → cursor 임시 전환 → pointer up 복귀 | 中 |
| C | GENERATE 후 패널 초기화 + 아이템 클릭 시 패널 복원 + 빈 캔버스 클릭 시 초기화 | 高 |
| D | VIEWPOINT 다이어그램 업로드 오버레이 (Q1 연동) | 低 |
| E | CanvasItem 타입 필드 확장 | 低 |

---

## Change A: 부모 삭제 시 자식 path cascade

### 현황

| 타입 | 삭제 로직 | motherId cascade |
|---|---|---|
| artboard | line 2669 | ❌ 없음 |
| sketch_generated | line 2719 | ❌ 없음 |
| upload / generated | line 2792 | ✅ 있음 |

### 변경

**artboard (line 2669)**:
```ts
// 현재
prev.filter((i: CanvasItem) => i.id !== item.id)
// 변경
prev.filter((i: CanvasItem) => i.id !== item.id && i.motherId !== item.id)
```

**sketch_generated (line 2719)**:
```ts
// 현재
prev.filter((i: CanvasItem) => i.id !== item.id)
// 변경
prev.filter((i: CanvasItem) => i.id !== item.id && i.motherId !== item.id)
```

---

## Change B: pen/eraser 모드 — 아트보드 밖 클릭 시 cursor 임시 전환

### 현황

pen/eraser 분기에서 drawTarget 없으면 `return;` → 아무 반응 없음

### 변경

**새 ref 추가** (line 338 근방 previousCanvasModeRef 옆):
```ts
const tempRestoreCanvasModeRef = useRef<'pen' | 'eraser' | null>(null); // V268: temp cursor fallback
```

**pen 분기 (line 922)**:
```ts
// 현재
if (!drawTarget) return;
// 변경
if (!drawTarget) {
  tempRestoreCanvasModeRef.current = 'pen';
  setCanvasMode('select');
  return;
}
```

**eraser 분기 (line 940)**:
```ts
// 현재
if (!eraseTarget) return;
// 변경
if (!eraseTarget) {
  tempRestoreCanvasModeRef.current = 'eraser';
  setCanvasMode('select');
  return;
}
```

**handleCanvasPointerUp 내 복귀 로직** (기존 정리 코드 이후):
```ts
// V268: Restore pen/eraser after temp cursor fallback
if (tempRestoreCanvasModeRef.current) {
  setCanvasMode(tempRestoreCanvasModeRef.current);
  tempRestoreCanvasModeRef.current = null;
}
```

> Q2 답변 반영: pointer up 시에만 복귀. pointer leave는 해당 없음.
> lasso 모드는 기존 `return` 유지 (요청 범위 外).

---

## Change C: 패널 상태 관리 (초기화 / 복원)

### 전체 흐름

```
GENERATE 클릭
  → 생성 완료 → 패널 초기화 (빈 상태) + 아이템 선택 해제
                                      ↕
              아이템 클릭 → 패널 열림 + 생성 시 파라미터 복원
                                      ↕
              빈 캔버스 클릭 → 패널 초기화
```

---

### C-1: CanvasItem 파라미터 저장 확장

**CHANGE VIEWPOINT `generated` 아이템 (line ~1664)**:
```ts
parameters: {
  angleIndex, altitudeIndex, lensIndex, timeIndex,
  analyzedOpticalParams: updatedOpticalParams,
  elevationParams: localElevationParams,
  sitePlanImage, elevationImages: localElevationImages,
  cvPrompt  // NEW
}
```

**SKETCH TO IMAGE `sketch_generated` 아이템 (line ~1848)**:
```ts
parameters: {
  angleIndex: 4, altitudeIndex: 2, lensIndex: 1, timeIndex: 2,
  analyzedOpticalParams: { angle: '06:00', altitude: '1.6m', lens: '24mm', time: '12:00' },
  elevationParams: null,
  sketchMode,           // NEW
  sketchStyle,          // NEW
  activeDetailStyle,    // NEW
  resolution,           // NEW
  aspectRatio           // NEW
}
```

**PLANNERS `text` 아이템 (line ~1915)**:
```ts
parameters: {
  ...,
  isPlannerResult: true,
  debateResult: result,
  plannerPrompt  // NEW
}
```

---

### C-2: GENERATE 후 패널 초기화

**CHANGE VIEWPOINT** — finally 블록 내 (`setIsGenerating(false)` 직후):
```ts
setCvPrompt('');
setSelectedItemIds([]);   // useEffect 발동 → sitePlanImage=null → 오버레이 재표시
```
현재 생성 완료 후 `setSelectedItemIds([newGenItem.id])` 호출(line 1686)을 제거하고
`setSelectedItemIds([])` 로 대체. 생성된 아이템은 canvas에 표시되나 선택 상태 없음.

**SKETCH TO IMAGE** — finally 블록 내:
```ts
setSketchMode('');
setSketchStyle(null);
setActiveDetailStyle(null);
setResolution('');
setAspectRatio(null);
setSelectedItemIds([]);
```
현재 `setSelectedItemIds([newGenItem.id])` (line 1862) → `setSelectedItemIds([])` 로 대체.

**PLANNERS** — 결과 카드 생성 후 (line 1927 이후):
```ts
setPlannerPrompt('');
setLastDebateResult(null);
setSelectedPlannerCardId(null);
```

---

### C-3: 아이템 클릭 시 패널 복원 (useEffect 확장)

기존 `useEffect` (line 474, dep: `[selectedItemIds]`)의 "아이템 선택됨" 분기(line 488+)에 추가:

```ts
// V268: Type-specific panel restore
if (item.type === 'generated') {
  setCvPrompt(item.parameters.cvPrompt || '');
  setSelectedFunction('CHANGE VIEWPOINT');
  setIsRightPanelOpen(true);
} else if (item.type === 'sketch_generated') {
  setSketchMode(item.parameters.sketchMode || '');
  setSketchStyle(item.parameters.sketchStyle ?? null);
  setActiveDetailStyle(item.parameters.activeDetailStyle ?? null);
  setResolution(item.parameters.resolution || '');
  setAspectRatio(item.parameters.aspectRatio ?? null);
  setSelectedFunction('SKETCH TO IMAGE');
  setIsRightPanelOpen(true);
} else if (item.parameters?.isPlannerResult) {
  setLastDebateResult(item.parameters.debateResult || null);
  setPlannerPrompt(item.parameters.plannerPrompt || '');
  // setSelectedFunction / setIsRightPanelOpen 은 카드 onClick에서 이미 처리됨
}
```

---

### C-4: 빈 캔버스 클릭 시 전체 패널 초기화

기존 `useEffect`의 "selectedItemIds 빈 배열" 분기(line 475~485)에 추가:

```ts
// V268: Reset all panel input states on canvas deselect
setCvPrompt('');
setSketchMode('');
setSketchStyle(null);
setActiveDetailStyle(null);
setResolution('');
setAspectRatio(null);
setPlannerPrompt('');
setLastDebateResult(null);
setSelectedPlannerCardId(null);
```

---

## Change D: VIEWPOINT 다이어그램 업로드 오버레이 (Q1 연동)

### Q1 답변 적용

오버레이 표시 조건: `sitePlanImage === null`

| 상태 | sitePlanImage | 오버레이 |
|---|---|---|
| 초기 / 패널 리셋 | null | 표시 |
| 이미지 로드 중(analyzeViewpoint) | set | 숨김 |
| generated 아이템 클릭 | item.parameters.sitePlanImage 복원 | 숨김 |
| GENERATE 완료 후 | useEffect 리셋 → null | 표시 |
| 빈 캔버스 클릭 | useEffect 리셋 → null | 표시 |

### 변경

`SitePlanDiagram` 컴포넌트 (line ~195 return JSX) 최상단 레이어에 추가:

```tsx
{/* V268: Upload Overlay — shown when no image loaded */}
{!sitePlanImage && (
  <div
    className="absolute inset-0 z-20 flex flex-col items-center justify-center pointer-events-none rounded-[inherit]"
    style={{ background: 'rgba(255,255,255,0.65)', backdropFilter: 'blur(6px)' }}
  >
    <Upload size={20} className="opacity-40 mb-2" />
    <span className="font-mono text-[10px] uppercase tracking-widest opacity-40 text-center leading-relaxed">
      UPLOAD<br />or DRAG & DROP
    </span>
  </div>
)}
```

- 캔버스 `upload/generated` 아이템 드래그 (`text/image-src`) → `handleDrop` → `onImageDrop` → `analyzeViewpoint` 흐름은 기존 구현 그대로 유지
- `isDragOver` 시 `ring-2 ring-blue-500` 효과도 기존 유지

---

## Change E: CanvasItem 타입 필드 확장 (line 275~301)

```ts
parameters: {
  angleIndex: number;
  altitudeIndex: number;
  lensIndex: number;
  timeIndex: number;
  analyzedOpticalParams?: any | null;
  elevationParams?: any | null;
  sitePlanImage?: string | null;
  elevationImages?: { ... } | null;
  // Path & Text
  points?: { x: number, y: number }[];
  strokeColor?: string;
  strokeWidth?: number;
  content?: string;
  fontSize?: number;
  color?: string;
  // Planners
  isPlannerResult?: boolean;
  debateResult?: DiscussionResult;
  plannerPrompt?: string;        // NEW V268
  // Sketch to Image
  sketchMode?: string;           // NEW V268
  sketchStyle?: string | null;   // NEW V268
  activeDetailStyle?: string | null; // NEW V268
  resolution?: string;           // NEW V268
  aspectRatio?: string | null;   // NEW V268
  // Change Viewpoint
  cvPrompt?: string;             // NEW V268
} | null;
```

---

## 적용 파일

`src/App.tsx` 단독 수정

## Q2 (확정)

pointer up 시에만 pen/eraser 복귀. ✅ Change B에 반영 완료.

---

## 승인 후 작업 순서

1. Change E — 타입 정의 확장
2. Change A — cascade delete 2곳
3. Change B — pen/eraser cursor fallback
4. Change C-1 — 생성 시 파라미터 저장 (3 아이템 타입)
5. Change C-2 — GENERATE 후 초기화 (3 패널)
6. Change C-3 — useEffect 아이템 선택 복원
7. Change C-4 — useEffect 빈 캔버스 초기화
8. Change D — VIEWPOINT 오버레이


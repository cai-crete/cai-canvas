# 작업지시서 V256
**작성일**: 2026-03-27
**대상 파일**: `src/App.tsx`
**기반 버전**: V255

---

## 개요

두께 패널 활성 버튼 스타일 수정(컨테이너 배경 제거 → 24px 독립 원),
펜 드로잉 미리보기 strokeWidth를 penStrokeWidth에 연동,
지우개 정밀 지우기: 현재 포인트-반경 방식 → 선분-원 교차 방식으로 교체.

---

## 변경 목록

| # | 항목 | 내용 |
|---|------|------|
| C-1 | 두께 패널 활성 원 | 컨테이너 bg 제거, 내부 원 24px로 변경 |
| C-2 | 펜 드로잉 미리보기 | 라이브 획 preview strokeWidth → penStrokeWidth 연동 |
| C-3 | 지우개 정밀 지우기 | 포인트 제거 방식 → 선분-원 교차 클리핑 방식으로 교체 |

---

## Q&A

| 질문 | 답변 |
|------|------|
| Q1. 활성 버튼 스타일 | B안: 컨테이너 배경 제거 + 내부 원 24px 독립 원 |
| Q2. 지우개 구간 사라짐 원인 | A안: 포인트 제거 시 인접 선분 전체가 사라짐 (선분-원 교차 계산 없음) |

---

## 원인 분석 (C-3)

현재 `eraseAtPoint`는 각 path 포인트가 지우개 반경 내에 있으면 해당 포인트를 제거합니다.

문제: 포인트 P[k]를 제거하면 P[k]와 연결된 두 선분(P[k-1]→P[k], P[k]→P[k+1])이 모두 사라져, 실제 지우개 원보다 훨씬 큰 구간이 제거됩니다.

```
지우개 원이 P[k] 근처에 닿은 경우:
 현재 동작: P[k-1] .............. [사라진 구간] ............... P[k+1]
 기대 동작: P[k-1] ── 정상선 ── [원 경계] (빈구간) [원 경계] ── 정상선 ── P[k+1]
```

**수정**: 선분 P[k-1]→P[k] 및 P[k]→P[k+1]이 지우개 원과 교차하는 점(t값)을 계산하여 원 경계에 새 포인트를 삽입, 원 안쪽 구간만 제거.

---

## 구현 계획

### 변경 C-1: 두께 패널 버튼 스타일 (펜·지우개 공통)

**변경 전** (활성 시 컨테이너 배경 적용, 내부 원 = size px):
```tsx
className={`w-8 h-8 flex items-center justify-center rounded-full transition-all hover:cursor-pointer ${
  penStrokeWidth === size
    ? 'bg-black dark:bg-white'
    : 'hover:bg-black/5 dark:hover:bg-white/5'
}`}
// 내부 div
style={{
  width: `${size}px`,
  height: `${size}px`,
  backgroundColor: penStrokeWidth === size
    ? (theme === 'dark' ? 'black' : 'white')
    : (theme === 'dark' ? 'white' : 'black'),
}}
```

**변경 후** (컨테이너 배경 없음, 활성 시 내부 원 24px):
```tsx
className="w-8 h-8 flex items-center justify-center rounded-full transition-all hover:cursor-pointer hover:bg-black/5 dark:hover:bg-white/5"
// 내부 div
style={{
  width: penStrokeWidth === size ? '24px' : `${size}px`,
  height: penStrokeWidth === size ? '24px' : `${size}px`,
  backgroundColor: theme === 'dark' ? 'white' : 'black',
}}
```

지우개 패널도 동일 구조 (`eraserStrokeWidth` 사용).

---

### 변경 C-2: 펜 드로잉 미리보기 strokeWidth

**위치**: SVG 오버레이 내 드로잉 중 polyline (~라인 1830)

```tsx
// 변경 전
strokeWidth={2}

// 변경 후
strokeWidth={penStrokeWidth}
```

---

### 변경 C-3: 지우개 정밀 지우기 — 선분-원 교차 클리핑

#### 3-A: 헬퍼 함수 2개 추가 (createPathItemFromAbsolutePoints 직전)

```typescript
// V256: 선분 P1→P2 와 원(center, radius) 의 교차 t값 반환 [t1, t2] (0≤t1≤t2≤1)
// 교차 없으면 null 반환
const segmentCircleIntersect = (
  p1: { x: number; y: number },
  p2: { x: number; y: number },
  c: { x: number; y: number },
  r: number
): [number, number] | null => {
  const dx = p2.x - p1.x, dy = p2.y - p1.y;
  const fx = p1.x - c.x, fy = p1.y - c.y;
  const a = dx * dx + dy * dy;
  if (a === 0) return null;
  const b = 2 * (fx * dx + fy * dy);
  const cv = fx * fx + fy * fy - r * r;
  const disc = b * b - 4 * a * cv;
  if (disc < 0) return null;
  const sq = Math.sqrt(disc);
  const t1 = (-b - sq) / (2 * a);
  const t2 = (-b + sq) / (2 * a);
  if (t2 < 0 || t1 > 1) return null;
  return [Math.max(0, t1), Math.min(1, t2)];
};

const lerpPt = (
  p1: { x: number; y: number },
  p2: { x: number; y: number },
  t: number
): { x: number; y: number } => ({
  x: p1.x + (p2.x - p1.x) * t,
  y: p1.y + (p2.y - p1.y) * t,
});
```

#### 3-B: eraseAtPoint 전체 교체

```typescript
const eraseAtPoint = (coords: { x: number; y: number }) => {
  setCanvasItems(prev => {
    const r = eraserStrokeWidth;
    let globalChanged = false;
    const newItems: CanvasItem[] = [];

    for (const item of prev) {
      if (item.type !== 'path') { newItems.push(item); continue; }

      const pts = (item.parameters?.points || []).map(p => ({
        x: item.x + p.x, y: item.y + p.y,
      }));
      if (pts.length < 2) continue;

      const inside = (p: { x: number; y: number }) =>
        (p.x - coords.x) ** 2 + (p.y - coords.y) ** 2 <= r * r;

      const outSegs: { x: number; y: number }[][] = [];
      let seg: { x: number; y: number }[] = [];
      let pathChanged = false;

      // 첫 포인트 처리
      if (!inside(pts[0])) {
        seg.push(pts[0]);
      } else {
        pathChanged = true;
      }

      for (let i = 1; i < pts.length; i++) {
        const p0 = pts[i - 1], p1 = pts[i];
        const p0in = inside(p0), p1in = inside(p1);
        const its = segmentCircleIntersect(p0, p1, coords, r);

        if (!p0in && !p1in) {
          if (its) {
            // 선분이 원을 통과: 진입점 추가 → 세그먼트 저장 → 탈출점부터 새 세그먼트
            pathChanged = true;
            seg.push(lerpPt(p0, p1, its[0]));
            if (seg.length >= 2) outSegs.push(seg);
            seg = [lerpPt(p0, p1, its[1]), p1];
          } else {
            seg.push(p1);
          }
        } else if (!p0in && p1in) {
          // 원 진입: 진입점에서 세그먼트 종료
          pathChanged = true;
          if (its) seg.push(lerpPt(p0, p1, its[0]));
          if (seg.length >= 2) outSegs.push(seg);
          seg = [];
        } else if (p0in && !p1in) {
          // 원 탈출: 탈출점부터 새 세그먼트 시작
          pathChanged = true;
          if (its) seg.push(lerpPt(p0, p1, its[1]));
          seg.push(p1);
        }
        // p0in && p1in: 둘 다 원 안 → skip (pathChanged 이미 true)
      }
      if (seg.length >= 2) outSegs.push(seg);

      if (!pathChanged) {
        newItems.push(item);
      } else {
        globalChanged = true;
        for (const s of outSegs) {
          if (s.length >= 2) newItems.push(createPathItemFromAbsolutePoints(s, item));
        }
      }
    }

    return globalChanged ? newItems : prev;
  });
};
```

---

## 예상 동작

| 상황 | 변경 전 | 변경 후 |
|------|---------|---------|
| 두께 패널 — 비활성 버튼 | `size`px 검정 원 | `size`px 검정 원 (동일) |
| 두께 패널 — 활성 버튼 | 32px 검정 배경 + `size`px 흰 원 | 24px 검정 원 (배경 없음) |
| 펜 드로잉 중 미리보기 선 | 항상 2px | 선택된 penStrokeWidth로 표시 |
| 지우개 — 선 위를 스침 | 스친 포인트 + 인접 선분 전체 사라짐 | 지우개 원 경계로 정확히 잘라내어 원 안쪽만 제거 |
| 지우개 — 두 포인트 사이 통과 | 아무것도 안 지워짐 | 원과 교차하는 선분 구간이 정확히 제거됨 |

---

## 비고

- `segmentCircleIntersect`, `lerpPt` 헬퍼 함수는 `createPathItemFromAbsolutePoints` 직전에 삽입
- 지우개 커서 원 반경 = `eraserStrokeWidth` (canvas coords) — 기존 V255와 동일
- 두께 패널 hover 효과는 활성/비활성 구분 없이 항상 적용
- C-2: 지우개에는 드로잉 미리보기 없음, 펜만 해당

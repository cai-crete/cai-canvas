# 작업지시서 V257
**작성일**: 2026-03-27
**대상 파일**: `src/App.tsx`
**기반 버전**: V256

---

## 개요

태블릿 핀치 줌 200% 확장, 업로드 버튼을 아트보드 생성 버튼으로 교체,
이미지 재생성 프로토콜을 GENERATE 버튼으로 이동, 아트보드 타입 신설,
sketch_generated 타입 신설 및 제어바 차별화, 스케치 범위 제한.

---

## 변경 목록

| # | 항목 | 내용 |
|---|------|------|
| C-1 | 태블릿 핀치 줌 | 최대 150% → 200% |
| C-2 | CanvasItem 타입 확장 | `'artboard'`, `'sketch_generated'` 추가; `src` nullable; `isLocked` 추가 |
| C-3 | 업로드 버튼 → 아트보드 버튼 | 빈 아트보드(595×842px) 즉시 생성 |
| C-4 | 아트보드 렌더링 | 스타일: rgba(248,244,232,0.5) + blur(2px) + shadow-xl |
| C-5 | 아트보드 제어바 | [업로드]|[LOCK]|[휴지통] |
| C-6 | 아트보드 이미지 업로드 | 아트보드 src 교체, width=595 fit |
| C-7 | 이미지 재생성 이동 | handleImageUpload V128 + analyzeViewpoint → handleGenerate로 이동 |
| C-8 | analyzeViewpoint 반환값 추가 | elevationParameters, elevationImages, analyzedOpt 반환 |
| C-9 | handleGenerate CHANGE VIEWPOINT | V128 → analyzeViewpoint → 뷰포인트 생성 순차 통합 |
| C-10 | 스케치 범위 제한 | artboard/upload/generated 위에서만 pen/eraser 시작 가능; 경계 이탈 시 포인트 클리핑 |
| C-11 | 라이브러리 탭 조건 | `type === 'generated'`에만 표시 (upload/artboard/sketch_generated 제외) |
| C-12 | SKETCH TO IMAGE 타입 변경 | 생성 결과 `'sketch_generated'`로 변경 |
| C-13 | sketch_generated 제어바 | [편집(Pencil)]|[DOWNLOAD]|[휴지통] (복제·라이브러리 없음) |
| C-14 | 편집 버튼 동작 | sketch_generated 아래 36px에 type='artboard'(src 포함) 복제 |

---

## Q&A

| 질문 | 답변 |
|------|------|
| Q1. 아트보드 생성 방식 | A안: 버튼 클릭 → 빈 아트보드(595×842) 즉시 생성 (파일 업로드 없이) |
| Q2. 아트보드 이미지 업로드 | 아트보드 자체의 src가 교체됨 |
| Q3. 스케치 가능 영역 | B안: 모든 upload/generated/artboard 타입 위에서 가능 |
| Q4. SKETCH TO IMAGE 타입 | A안: 새 타입 'sketch_generated' 추가 |
| Q5. GENERATE 순서 | A안: 재생성 먼저 → 재생성 이미지로 뷰포인트 생성 |
| Q6. 편집 버튼 아이콘 | Lucide `Pencil` 사용 |
| Q7. CHANGE VIEWPOINT 업로드 | C안: 아트보드 [업로드]로 올린 이미지를 CHANGE VIEWPOINT에서도 사용 |
| Q8. 편집 복제 이미지 타입 | A안: type='artboard' (src 포함) → 스케치 가능 |
| Q9. 스케치 경계 제한 | 경계 밖 pointerDown 차단 + 경계 이탈 포인트 클리핑 |

---

## 구현 계획

---

### 변경 C-1: 태블릿 핀치 줌 150→200

**위치**: `handleTouchMove` (~line 1090)

```typescript
// 변경 전
setCanvasZoom(prev => Math.min(Math.max(prev + dist, 10), 150));
// 변경 후
setCanvasZoom(prev => Math.min(Math.max(prev + dist, 10), 200));
```

---

### 변경 C-2: CanvasItem 인터페이스 확장

**위치**: `interface CanvasItem` (~line 234)

```typescript
interface CanvasItem {
  id: string;
  type: 'upload' | 'generated' | 'artboard' | 'sketch_generated' | 'path' | 'text';
  src: string | null;  // artboard는 src 없이 시작
  x: number;
  y: number;
  width: number;
  height: number;
  motherId: string | null;
  isLocked?: boolean;  // artboard 잠금 상태
  parameters: { ... } | null;  // 기존 유지
}
```

---

### 변경 C-3: 업로드 버튼 → 아트보드 생성 버튼

**위치**: 좌측 툴바 업로드 버튼 (~line 2178)

**변경 전**: `onClick={() => fileInputRef.current?.click()}` + `<input ... onChange={handleImageUpload} />`

**변경 후**: 클릭 시 빈 아트보드 아이템 생성, file input 제거

```tsx
// 버튼 변경
<button
  onClick={() => {
    const artboardId = `artboard-${Date.now()}`;
    let newX = -595 / 2;
    let newY = -842 / 2;
    if (canvasItems.length > 0) {
      const leftMost = canvasItems.reduce((p, c) => p.x < c.x ? p : c);
      const bottomMost = canvasItems.reduce((p, c) =>
        p.y + p.height > c.y + c.height ? p : c
      );
      newX = leftMost.x;
      newY = bottomMost.y + bottomMost.height + 40;
    }
    const newArtboard: CanvasItem = {
      id: artboardId,
      type: 'artboard',
      src: null,
      x: newX,
      y: newY,
      width: 595,
      height: 842,
      motherId: null,
      parameters: null,
      isLocked: false,
    };
    setHistoryStates(prev => [...prev, canvasItems]);
    setCanvasItems(prev => [...prev, newArtboard]);
  }}
  className="w-11 h-11 flex items-center justify-center rounded-full bg-black text-white hover:bg-neutral-800 transition-colors shadow-lg pointer-events-auto hover:cursor-pointer"
  title="Upload Artboard"
>
  {/* 기존 SVG 아이콘 유지 */}
</button>
// <input> 태그 제거 (fileInputRef, handleImageUpload 더 이상 불필요)
```

**추가**: 아트보드 업로드 전용 ref 및 상태 추가 (C-5에서 사용)

```typescript
const artboardFileInputRef = useRef<HTMLInputElement>(null);
const pendingArtboardIdRef = useRef<string | null>(null);
```

---

### 변경 C-4: 아트보드 렌더링

**위치**: 캔버스 아이템 렌더링 루프 (~line 2226), 이미지 렌더링 분기에 아트보드 추가

```tsx
{item.type === 'artboard' ? (
  <div
    className="w-full h-full shadow-xl"
    style={{
      backgroundColor: 'rgba(248, 244, 232, 0.5)',
      backdropFilter: 'blur(2px)',
    }}
  >
    {item.src && (
      <img
        src={item.src}
        alt="artboard"
        className="w-full h-full object-contain pointer-events-none"
        draggable={false}
      />
    )}
  </div>
) : (
  // 기존 img 렌더링
  <img ... />
)}
```

**아트보드 라벨**: type='artboard'는 기존 ORIGINAL/VIEWPOINT 라벨 표시 제외

```tsx
// 라벨 렌더링 조건에 artboard 추가 (이미 path, text 제외 있음)
if (item.type === 'path' || item.type === 'text' || item.type === 'artboard') return null;
```

**아트보드 잠금 시 pointerEvents 처리**:

```tsx
<div
  key={item.id}
  style={{
    ...
    pointerEvents: (canvasMode === 'pan' || (item.type === 'artboard' && item.isLocked)) ? 'none' : 'auto',
  }}
>
```

---

### 변경 C-5: 아트보드 제어바 — [업로드]|[LOCK]|[휴지통]

**위치**: 선택 오버레이 내 제어바 (~line 2317), 아이템 타입별 분기 추가

```tsx
{/* 아트보드 전용 제어바 */}
{item.type === 'artboard' && (
  <div
    className={`absolute flex items-center bg-white/70 dark:bg-black/70 backdrop-blur-md z-[40] px-1.5 rounded-full pointer-events-auto`}
    style={{
      top: `-${56 / (canvasZoom / 100)}px`,
      right: 0,
      height: `${44 / (canvasZoom / 100)}px`,
      boxShadow: '0 4px 16px rgba(0,0,0,0.08)'
    }}
    onPointerDown={(e) => e.stopPropagation()}
  >
    {/* 업로드 버튼 */}
    <button
      onClick={() => {
        pendingArtboardIdRef.current = item.id;
        artboardFileInputRef.current?.click();
      }}
      className="flex items-center justify-center hover:bg-black/5 dark:hover:bg-white/5 transition-colors rounded-full"
      style={{ width: `${36 / (canvasZoom / 100)}px`, height: `${36 / (canvasZoom / 100)}px` }}
      title="이미지 업로드"
    >
      <Upload size={12 / (canvasZoom / 100)} />
    </button>
    <div className="w-[1px] bg-black/10 dark:bg-white/10 mx-0.5" style={{ height: (28 / (canvasZoom / 100)) + 'px' }} />
    {/* LOCK 버튼 */}
    <button
      onClick={() => {
        setCanvasItems(prev => prev.map(i =>
          i.id === item.id ? { ...i, isLocked: !i.isLocked } : i
        ));
      }}
      className={`flex items-center justify-center transition-colors rounded-full ${item.isLocked ? 'bg-black/10 dark:bg-white/10' : 'hover:bg-black/5 dark:hover:bg-white/5'}`}
      style={{ width: `${36 / (canvasZoom / 100)}px`, height: `${36 / (canvasZoom / 100)}px` }}
      title={item.isLocked ? "잠금 해제" : "잠금"}
    >
      {/* Lock/Unlock SVG (inline) */}
      {item.isLocked
        ? <svg width={12/(canvasZoom/100)} height={12/(canvasZoom/100)} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
        : <svg width={12/(canvasZoom/100)} height={12/(canvasZoom/100)} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 9.9-1"/></svg>
      }
    </button>
    <div className="w-[1px] bg-black/10 dark:bg-white/10 mx-0.5" style={{ height: (28 / (canvasZoom / 100)) + 'px' }} />
    {/* 휴지통 */}
    <button
      onClick={() => {
        setHistoryStates(prevH => [...prevH, canvasItems]);
        setRedoStates([]);
        setCanvasItems(prev => prev.filter(i => i.id !== item.id));
        setSelectedItemIds([]);
      }}
      className="flex items-center justify-center hover:bg-black/5 dark:hover:bg-white/5 transition-colors text-red-500 rounded-full"
      style={{ width: `${36 / (canvasZoom / 100)}px`, height: `${36 / (canvasZoom / 100)}px` }}
      title="삭제"
    >
      <Trash2 size={12 / (canvasZoom / 100)} />
    </button>
  </div>
)}

{/* 기존 제어바: artboard 타입 제외 */}
{item.type !== 'artboard' && (
  /* 기존 제어바 코드 유지 */
)}
```

**아트보드 파일 입력 (JSX 하단)**:

```tsx
<input
  type="file"
  ref={artboardFileInputRef}
  onChange={handleArtboardImageUpload}
  accept="image/*"
  className="hidden"
/>
```

---

### 변경 C-6: handleArtboardImageUpload 함수 추가

**위치**: `toggleTheme` 함수 직후, 기존 `handleImageUpload` 위치에 대체

```typescript
const handleArtboardImageUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
  const artboardId = pendingArtboardIdRef.current;
  const file = e.target.files?.[0];
  if (!artboardId || !file) return;
  e.target.value = ''; // 동일 파일 재선택 가능하도록 초기화

  const reader = new FileReader();
  reader.onloadend = () => {
    const base64Image = reader.result as string;
    const img = new Image();
    img.onload = () => {
      // 아트보드 폭(595px)에 맞춰 높이 비례 계산
      const newHeight = Math.round(img.height * (595 / img.width));
      setCanvasItems(prev => prev.map(i =>
        i.id === artboardId
          ? { ...i, src: base64Image, height: newHeight }
          : i
      ));
    };
    img.src = base64Image;
  };
  reader.readAsDataURL(file);
};
```

**기존 `handleImageUpload` 함수**: 완전 제거 (fileInputRef도 제거)

---

### 변경 C-7~C-9: 이미지 재생성 + 분석 + 뷰포인트 생성 통합

#### C-8: analyzeViewpoint 반환값 추가

**위치**: `analyzeViewpoint` 함수 내 `runAnalysis` (~line 1258)

`runAnalysis` 함수가 성공 시 `{ elevationParameters, analyzedOpt, elevationImages }` 반환:

```typescript
// runAnalysis 내부, return true 직전에 추가
// generateElevations를 await하고 반환값 수집
const elImages = await generateElevationsAndReturn(base64Image, data.elevation_parameters, itemId);

return {
  elevationParameters: data.elevation_parameters,
  analyzedOpt,
  elevationImages: elImages,
};
```

`generateElevations`도 elevationImages를 반환하도록 수정:

```typescript
// generateElevations 함수: 기존 setElevationImages(images) 유지 + return images 추가
return images; // { front, right, rear, left, top }
```

`analyzeViewpoint` 반환 타입:

```typescript
const analyzeViewpoint = async (...): Promise<{
  elevationParameters: any;
  analyzedOpt: any;
  elevationImages: any;
} | null>
```

#### C-9: handleGenerate CHANGE VIEWPOINT 수정

**위치**: `handleGenerate` CHANGE VIEWPOINT 분기 (~line 1444)

**변경 전 흐름**: elevationParams 검증 → 기존 elevationImages 사용 → 뷰포인트 생성

**변경 후 흐름**:
1. sourceItem이 artboard/upload/generated 인지 확인 (src 존재 여부 포함)
2. 이미 분석된 경우(`sourceItem.parameters?.elevationParams` 존재): 분석 건너뜀
3. 최초 GENERATE 시: V128 재생성 → analyzeViewpoint → 로컬 params 획득
4. 로컬 params로 뷰포인트 생성

```typescript
// handleGenerate CHANGE VIEWPOINT 분기 전체 수정

// 0. 소스 아이템 확인 (artboard 타입도 허용)
const sourceItem = selectedItemIds[0]
  ? canvasItems.find(item => item.id === selectedItemIds[0])
  : canvasItems.find(item => item.type === 'artboard' || item.type === 'upload');

if (!sourceItem || !sourceItem.src) {
  alert("아트보드에 이미지를 먼저 업로드하세요.");
  return;
}

setIsGenerating(true);
const controller = new AbortController();
abortControllerRef.current = controller;

try {
  let activeSrc = sourceItem.src;
  let localElevationParams = sourceItem.parameters?.elevationParams || null;
  let localElevationImages = sourceItem.parameters?.elevationImages || elevationImages;
  let localAnalyzedOpt = sourceItem.parameters?.analyzedOpticalParams || analyzedOpticalParams;

  // Step 1: 최초 GENERATE — V128 재생성 + 분석
  if (!localElevationParams) {
    setAnalysisStep('배경 이미지 최적화 중...');
    const ai_regen = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });
    try {
      const regenResp = await ai_regen.models.generateContent({
        model: IMAGE_GEN,
        contents: {
          parts: [
            { inlineData: { data: activeSrc.split(',')[1], mimeType: activeSrc.split(';')[0].split(':')[1] } },
            { text: "Recreate and enhance this architectural image exactly as it is without altering its geometry or perspective." }
          ]
        }
      });
      if (regenResp.candidates?.[0]?.content?.parts) {
        for (const part of regenResp.candidates[0].content.parts) {
          if (part.inlineData) {
            activeSrc = `data:${part.inlineData.mimeType};base64,${part.inlineData.data}`;
            setCanvasItems(prev => prev.map(i =>
              i.id === sourceItem.id ? { ...i, src: activeSrc } : i
            ));
            break;
          }
        }
      }
    } catch (regenErr) {
      console.warn('[V257] V128 regen failed, using original:', regenErr);
    }

    // Step 2: analyzeViewpoint (returns params)
    const analysisResult = await analyzeViewpoint(activeSrc, sourceItem.id);
    if (!analysisResult) {
      setIsGenerating(false);
      return;
    }
    localElevationParams = analysisResult.elevationParameters;
    localElevationImages = analysisResult.elevationImages;
    localAnalyzedOpt = analysisResult.analyzedOpt;
  }

  // Step 3: 기존 검증 및 뷰포인트 생성 (elevationParams → localElevationParams 로 교체)
  const isValidPhase3 = ANGLES[angleIndex] && ALTITUDES[altitudeIndex] && LENSES[lensIndex] && TIMES[timeIndex];
  if (!isValidPhase3) { ... return; }

  // 이하 기존 코드에서 elevationParams → localElevationParams,
  //                        elevationImages → localElevationImages,
  //                        analyzedOpticalParams → localAnalyzedOpt 로 변수명 교체
  // 나머지 생성 로직은 그대로 유지
```

**기존 elevationParams 검증 블록 제거**:
```typescript
// 제거할 코드 (lines ~1463-1468)
const hasEnsemblePair = sourceItem.src && elevationParams && ...;
if (!hasEnsemblePair) { alert(...); return; }
```

---

### 변경 C-10: 스케치 범위 제한

#### 10-A: drawableBounds 헬퍼 함수 추가

```typescript
// 스케치 가능한 아이템 바운딩박스 목록 반환
const getDrawableItemBounds = () =>
  canvasItems
    .filter(i => i.type === 'artboard' || i.type === 'upload' || i.type === 'generated')
    .map(i => ({ x: i.x, y: i.y, w: i.width, h: i.height }));

const isInsideAnyDrawable = (pt: { x: number; y: number }) =>
  getDrawableItemBounds().some(b =>
    pt.x >= b.x && pt.x <= b.x + b.w && pt.y >= b.y && pt.y <= b.y + b.h
  );
```

#### 10-B: handlePointerDown — 펜 모드 시작 차단

```typescript
// pen 모드 블록 진입부에 추가
} else if (canvasMode === 'pen') {
  if (!isInsideAnyDrawable(coords)) return; // 경계 밖 시작 차단
  // 기존 코드 유지
  setIsDrawing(true);
  activeDrawBoundsRef.current = getDrawableItemBounds().find(b =>
    coords.x >= b.x && coords.x <= b.x + b.w &&
    coords.y >= b.y && coords.y <= b.y + b.h
  ) || null;
  ...
```

**추가 Ref**:
```typescript
const activeDrawBoundsRef = useRef<{ x: number; y: number; w: number; h: number } | null>(null);
```

#### 10-C: handlePointerMove — 경계 이탈 포인트 클리핑

```typescript
// pen 드로잉 중 포인트 추가 전 경계 체크
if (isDrawingRef.current) {
  const bounds = activeDrawBoundsRef.current;
  if (bounds) {
    // 경계 안에 있는 포인트만 추가
    if (
      coords.x >= bounds.x && coords.x <= bounds.x + bounds.w &&
      coords.y >= bounds.y && coords.y <= bounds.y + bounds.h
    ) {
      setCurrentStroke(prev => [...prev, coords]);
    }
    // 경계 밖 포인트는 무시 (클리핑)
  } else {
    setCurrentStroke(prev => [...prev, coords]);
  }
}
```

---

### 변경 C-11: 라이브러리 탭 표시 조건 변경

**위치**: 제어바 내 Book 아이콘 버튼 (~line 2367)

**변경 전**: 모든 non-artboard 아이템에 표시 (upload는 disabled)

**변경 후**: `type === 'generated'`에만 표시

```tsx
// 변경 전
<button ... disabled={!item.parameters?.analyzedOpticalParams && item.type !== 'generated'}>
  <Book ... />
</button>

// 변경 후
{item.type === 'generated' && (
  <button
    onClick={() => { ... }}
    className={`...`}
    style={...}
    title="라이브러리 (아트보드)"
  >
    <Book size={12 / (canvasZoom / 100)} />
  </button>
)}
```

구분자(`<div className="w-[1px] ...">`)도 generated 조건에 포함.

---

### 변경 C-12: SKETCH TO IMAGE 생성 타입 변경

**위치**: handleGenerate SKETCH TO IMAGE 분기 (~line 1693)

```typescript
// 변경 전
type: 'generated',
id: `sketch-${Date.now()}`,

// 변경 후
type: 'sketch_generated',
id: `sketch-${Date.now()}`,
```

---

### 변경 C-13 & C-14: sketch_generated 제어바

**위치**: 제어바 분기 (C-5에서 아트보드 제어바를 분리했으므로, 기존 제어바에도 sketch_generated 분기 추가)

```tsx
{/* sketch_generated 전용 제어바: [편집]|[DOWNLOAD]|[휴지통] */}
{item.type === 'sketch_generated' && selectedItemIds.includes(item.id) && (
  <div
    className="absolute flex items-center bg-white/70 dark:bg-black/70 backdrop-blur-md z-[40] px-1.5 rounded-full pointer-events-auto"
    style={{
      top: `-${56 / (canvasZoom / 100)}px`,
      right: 0,
      height: `${44 / (canvasZoom / 100)}px`,
      boxShadow: '0 4px 16px rgba(0,0,0,0.08)'
    }}
    onPointerDown={(e) => e.stopPropagation()}
  >
    {/* 편집 버튼 */}
    <button
      onClick={() => {
        const editArtboard: CanvasItem = {
          id: `artboard-${Date.now()}`,
          type: 'artboard',
          src: item.src,
          x: item.x,
          y: item.y + item.height + 36,
          width: item.width,
          height: item.height,
          motherId: item.id,
          parameters: null,
          isLocked: false,
        };
        setCanvasItems(prev => [...prev, editArtboard]);
      }}
      className="flex items-center justify-center hover:bg-black/5 dark:hover:bg-white/5 transition-colors rounded-full"
      style={{ width: `${36 / (canvasZoom / 100)}px`, height: `${36 / (canvasZoom / 100)}px` }}
      title="편집 (아트보드로 복제)"
    >
      <Pencil size={12 / (canvasZoom / 100)} />
    </button>
    <div className="w-[1px] bg-black/10 dark:bg-white/10 mx-0.5" style={{ height: (28 / (canvasZoom / 100)) + 'px' }} />
    {/* 다운로드 */}
    <a
      href={item.src || '#'}
      download="sketch_result.png"
      className="flex items-center justify-center hover:bg-black/5 dark:hover:bg-white/5 transition-colors rounded-full"
      style={{ width: `${36 / (canvasZoom / 100)}px`, height: `${36 / (canvasZoom / 100)}px` }}
      title="다운로드"
    >
      <Download size={14 / (canvasZoom / 100)} />
    </a>
    <div className="w-[1px] bg-black/10 dark:bg-white/10 mx-0.5" style={{ height: (28 / (canvasZoom / 100)) + 'px' }} />
    {/* 휴지통 */}
    <button
      onClick={() => {
        setHistoryStates(prevH => [...prevH, canvasItems]);
        setRedoStates([]);
        setCanvasItems(prev => prev.filter(i => i.id !== item.id));
        setSelectedItemIds([]);
      }}
      className="flex items-center justify-center hover:bg-black/5 dark:hover:bg-white/5 transition-colors text-red-500 rounded-full"
      style={{ width: `${36 / (canvasZoom / 100)}px`, height: `${36 / (canvasZoom / 100)}px` }}
      title="삭제"
    >
      <Trash2 size={12 / (canvasZoom / 100)} />
    </button>
  </div>
)}

{/* 기존 제어바: upload/generated/artboard(이미 별도) 이외 타입 제외 조건 추가 */}
{(item.type === 'upload' || item.type === 'generated') && selectedItemIds.includes(item.id) && (
  /* 기존 제어바 코드 */
)}
```

**sketch_generated 드로잉 불가**: C-10의 `getDrawableItemBounds`에서 `sketch_generated` 제외 (이미 artboard/upload/generated만 포함하므로 자동 처리)

---

## 예상 동작

| 상황 | 변경 전 | 변경 후 |
|------|---------|---------|
| 태블릿 핀치 줌 | 150% 최대 | 200% 최대 |
| 업로드 버튼 클릭 | 파일 선택 다이얼로그 | 빈 아트보드(595×842) 즉시 생성 |
| 이미지 업로드 후 | 즉시 V128 재생성 + 분석 시작 | 아무 API 없음, 즉시 표시 |
| GENERATE (최초) | 기존 elevationParams 없으면 오류 | V128 재생성 → 분석 → 뷰포인트 생성 |
| GENERATE (재클릭) | 동일 | 분석 건너뜀, 뷰포인트만 생성 |
| 빈 캔버스에서 펜 드래그 | 자유 드로잉 | 차단됨 |
| 아트보드/이미지 위 펜 드래그 | 자유 드로잉 | 허용 (경계 내) |
| 경계 밖으로 드래그 | 계속 그려짐 | 경계 바깥 포인트 클리핑 |
| SKETCH TO IMAGE 결과 제어바 | 복제/라이브러리/다운로드/삭제 | 편집/다운로드/삭제 |
| sketch_generated 위 펜 | 드로잉 가능 | 차단됨 |
| sketch_generated 편집 버튼 | 없음 | 아래 36px에 artboard 복제 |
| 라이브러리 탭 표시 | upload(분석후) + generated | generated(CHANGE VIEWPOINT)만 |

---

## 비고

- `fileInputRef` 및 `handleImageUpload` 완전 제거 (아트보드 업로드로 대체)
- `Pencil` 아이콘은 기존 import (`import { ..., Pencil, ... }`)에 이미 포함
- `Upload` 아이콘도 기존 import에 포함
- Lock/Unlock 아이콘은 Lucide `Lock`, `LockOpen` 사용 — import에 추가 필요
- `analyzeViewpoint` 반환값 수정 시 TypeScript 타입 체크 통과 확인 필수
- 기존 type='upload' 아이템(이전 버전에서 생성된 것)은 하위 호환성 유지

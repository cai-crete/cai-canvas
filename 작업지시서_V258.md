# 작업지시서 V258
**작성일**: 2026-03-27
**대상 파일**: `src/App.tsx`
**기반 버전**: V257

---

## 개요

아트보드 치수 가로(960×540) 변경, 잠금 기능 수정, path/text motherId 연동 및 아트보드와 함께 이동,
SKETCH TO IMAGE GENERATE 활성화 조건 추가, 아트보드 합성 이미지 전송,
CHANGE VIEWPOINT GENERATE 버튼 아트보드 허용.

---

## 변경 목록

| # | 항목 | 내용 |
|---|------|------|
| C-1 | 아트보드 치수 변경 | 595×842 → 960×540 (16:9 가로) |
| C-2 | 잠금 기능 수정 | CSS pointerEvents 제거 → JS 레벨 드래그/리사이즈 차단 |
| C-3 | activeDrawTargetIdRef 추가 | pen/text 시작 시 대상 아이템 ID 저장 |
| C-4 | text 모드 아트보드 제한 | 아트보드 내부에서만 텍스트 시작, motherId 저장 |
| C-5 | path/text motherId 저장 | handlePointerUp에서 motherId = activeDrawTargetIdRef |
| C-6 | 아트보드 이동 시 자식 이동 | clickedItemStartPositions에 motherId 자식 포함 + drag 시 함께 이동 |
| C-7 | SKETCH TO IMAGE GENERATE 조건 | artboard/upload/generated 선택 시에만 버튼 활성화 |
| C-8 | 아트보드 합성 이미지 전송 | sourceItem이 artboard → canvas composite (배경+path+text) 생성 후 전송 |
| C-9 | CHANGE VIEWPOINT GENERATE 버튼 | artboard 선택 시 disabled 해제 |

---

## 구현 계획

---

### C-1: 아트보드 치수 960×540

**위치 1** — 아트보드 생성 버튼 (~line 2159)

```tsx
// 변경 전
let newX = -595 / 2;
let newY = -842 / 2;
...
width: 595,
height: 842,

// 변경 후
let newX = -960 / 2;
let newY = -540 / 2;
...
width: 960,
height: 540,
```

**위치 2** — handleArtboardImageUpload (~line 1156)

```typescript
// 변경 전
const newHeight = Math.round(img.height * (595 / img.width));

// 변경 후
const newHeight = Math.round(img.height * (960 / img.width));
```

---

### C-2: 잠금 기능 수정

**문제**: `pointerEvents: (canvasMode === 'pan' || (item.type === 'artboard' && item.isLocked)) ? 'none' : 'auto'`
→ 잠금 시 컨트롤 바까지 클릭 불가 (unlock 불가능).

**위치 1** — 아이템 컨테이너 CSS (~line 2242)

```tsx
// 변경 전
pointerEvents: (canvasMode === 'pan' || (item.type === 'artboard' && item.isLocked)) ? 'none' : 'auto',

// 변경 후
pointerEvents: canvasMode === 'pan' ? 'none' : 'auto',
```

**위치 2** — handlePointerDown select mode `clickedItem` 찾기 (~line 801)

```typescript
// 변경 전
const clickedItem = [...canvasItems].reverse().find((item: CanvasItem) => {
  if (item.type === 'path') return false;
  return coords.x >= item.x && ...

// 변경 후
const clickedItem = [...canvasItems].reverse().find((item: CanvasItem) => {
  if (item.type === 'path') return false;
  if (item.type === 'artboard' && item.isLocked) return false; // V258: locked artboard skip
  return coords.x >= item.x && ...
```

**위치 3** — handlePointerDown resize 시작 시 locked 체크 (~line 784)

```typescript
// 변경 전
if (item) {
  setHistoryStates(prev => [...prev, canvasItems]);

// 변경 후
if (item && !(item.type === 'artboard' && item.isLocked)) {
  setHistoryStates(prev => [...prev, canvasItems]);
```

---

### C-3: activeDrawTargetIdRef 추가

**위치** — refs 섹션 (~line 417)

```typescript
const activeDrawBoundsRef = useRef<{ x: number; y: number; width: number; height: number } | null>(null);
const activeDrawTargetIdRef = useRef<string | null>(null); // V258: artboard/item ID for motherId linkage
```

---

### C-4: text 모드 아트보드 내부 제한

**위치** — handlePointerDown text 블록 (~line 874)

```typescript
// 변경 전
} else if (canvasMode === 'text') {
  setIsCreatingText(true);
  isCreatingTextRef.current = true;
  setTextStartPos(coords);
  e.currentTarget.setPointerCapture(e.pointerId);

// 변경 후
} else if (canvasMode === 'text') {
  // V258: Restrict text to artboard/image bounds
  const textTarget = canvasItems.find((item: CanvasItem) =>
    (item.type === 'upload' || item.type === 'generated' || item.type === 'artboard') &&
    coords.x >= item.x && coords.x <= item.x + item.width &&
    coords.y >= item.y && coords.y <= item.y + item.height
  );
  if (!textTarget) return;
  activeDrawTargetIdRef.current = textTarget.id;
  setIsCreatingText(true);
  isCreatingTextRef.current = true;
  setTextStartPos(coords);
  e.currentTarget.setPointerCapture(e.pointerId);
```

---

### C-5: path/text motherId 저장

**위치 1** — handlePointerDown pen 블록: drawTarget.id 저장 (~line 868)

```typescript
if (!drawTarget) return;
activeDrawBoundsRef.current = { x: drawTarget.x, y: drawTarget.y, width: drawTarget.width, height: drawTarget.height };
activeDrawTargetIdRef.current = drawTarget.id; // V258
```

**위치 2** — handlePointerDown eraser 블록: eraseTarget.id 저장 (불필요, 지우개는 motherId 무관)
→ 지우개는 activeDrawTargetIdRef 저장 불필요.

**위치 3** — handlePointerUp path 생성 (~line 1023)

```typescript
// 변경 전
motherId: null,

// 변경 후
motherId: activeDrawTargetIdRef.current,  // V258: link to artboard/item
```

**위치 4** — handlePointerUp text 생성 (~line 1052)

```typescript
// 변경 전
motherId: null,

// 변경 후
motherId: activeDrawTargetIdRef.current,  // V258: link to artboard/item
```

**위치 5** — handlePointerUp 클리어 (~line 1076)

```typescript
isErasingRef.current = false;
activeDrawBoundsRef.current = null;
activeDrawTargetIdRef.current = null; // V258
```

**위치 6** — text 생성 시 아트보드 bounds 클리핑 (~line 1048)

```typescript
// 변경 전
x: Math.min(coords.x, textStartPos.x),
y: Math.min(coords.y, textStartPos.y),
width: width,
height: height,

// 변경 후 — artboard bounds로 클리핑
const targetItem = canvasItems.find(i => i.id === activeDrawTargetIdRef.current);
const clampedX = targetItem
  ? Math.max(targetItem.x, Math.min(coords.x, textStartPos.x))
  : Math.min(coords.x, textStartPos.x);
const clampedY = targetItem
  ? Math.max(targetItem.y, Math.min(coords.y, textStartPos.y))
  : Math.min(coords.y, textStartPos.y);
const clampedW = targetItem
  ? Math.min(width, targetItem.x + targetItem.width - clampedX)
  : width;
const clampedH = targetItem
  ? Math.min(height, targetItem.y + targetItem.height - clampedY)
  : height;
...
x: clampedX,
y: clampedY,
width: clampedW,
height: clampedH,
```

---

### C-6: 아트보드 이동 시 motherId 자식 아이템 함께 이동

**위치 1** — clickedItemStartPositionsRef 구축 시 자식 포함 (~line 837)

```typescript
// 변경 전
clickedItemStartPositionsRef.current = currentSelection.reduce((acc, id) => {
  const itm = canvasItems.find(i => i.id === id);
  if (itm) acc[id] = { x: itm.x, y: itm.y };
  return acc;
}, {} as Record<string, { x: number, y: number }>);

// 변경 후
const startPositions: Record<string, { x: number, y: number }> = {};
for (const id of currentSelection) {
  const itm = canvasItems.find(i => i.id === id);
  if (itm) {
    startPositions[id] = { x: itm.x, y: itm.y };
    // V258: Also store positions of motherId-linked children (path/text)
    if (itm.type === 'artboard') {
      canvasItems.forEach(child => {
        if (child.motherId === id && (child.type === 'path' || child.type === 'text')) {
          startPositions[child.id] = { x: child.x, y: child.y };
        }
      });
    }
  }
}
clickedItemStartPositionsRef.current = startPositions;
```

**위치 2** — handlePointerMove drag 시 자식 함께 이동 (~line 938)

```typescript
// 변경 전
setCanvasItems(prev => prev.map(item => {
  if (!currentIds.includes(item.id)) return item;
  const startPos = clickedItemStartPositionsRef.current[item.id];
  return startPos ? { ...item, x: startPos.x + dx, y: startPos.y + dy } : item;
}));

// 변경 후
setCanvasItems(prev => prev.map(item => {
  const startPos = clickedItemStartPositionsRef.current[item.id];
  if (!startPos) return item;
  // selected item OR motherId-linked child
  if (currentIds.includes(item.id) || item.type === 'path' || item.type === 'text') {
    return { ...item, x: startPos.x + dx, y: startPos.y + dy };
  }
  return item;
}));
```

---

### C-7: SKETCH TO IMAGE GENERATE 버튼 활성화 조건

**위치** — SKETCH TO IMAGE GENERATE 버튼 disabled 조건 (~line 3005)

```tsx
// 변경 전
disabled={!sketchMode || !sketchStyle || isGenerating}

// 변경 후
disabled={
  !sketchMode || !sketchStyle || isGenerating ||
  !selectedItemIds.length ||
  !canvasItems.find(i =>
    i.id === selectedItemIds[0] &&
    (i.type === 'artboard' || i.type === 'upload' || i.type === 'generated')
  )
}
```

---

### C-8: 아트보드 합성 이미지 전송

**위치 1** — handleGenerate 내 `createArtboardComposite` 헬퍼 함수 추가 (handleGenerate 바로 위, ~line 1410)

```typescript
// V258: Artboard composite image generator
const createArtboardComposite = (artboard: CanvasItem): Promise<string | null> => {
  return new Promise((resolve) => {
    const canvas = document.createElement('canvas');
    canvas.width = artboard.width;
    canvas.height = artboard.height;
    const ctx = canvas.getContext('2d');
    if (!ctx) { resolve(null); return; }

    // 1. Background
    ctx.fillStyle = 'rgba(248, 244, 232, 1)';
    ctx.fillRect(0, 0, canvas.width, canvas.height);

    const drawPathsAndText = () => {
      // 3. Path items
      const pathItems = canvasItems.filter(i => i.type === 'path' && i.motherId === artboard.id);
      for (const pathItem of pathItems) {
        const pts: { x: number; y: number }[] = pathItem.parameters?.points || [];
        if (pts.length < 2) continue;
        ctx.beginPath();
        ctx.strokeStyle = pathItem.parameters?.strokeColor || '#000000';
        ctx.lineWidth = pathItem.parameters?.strokeWidth || 2;
        ctx.lineCap = 'round';
        ctx.lineJoin = 'round';
        const ox = pathItem.x - artboard.x;
        const oy = pathItem.y - artboard.y;
        ctx.moveTo(ox + pts[0].x, oy + pts[0].y);
        for (let k = 1; k < pts.length; k++) ctx.lineTo(ox + pts[k].x, oy + pts[k].y);
        ctx.stroke();
      }
      // 4. Text items
      const textItems = canvasItems.filter(i => i.type === 'text' && i.motherId === artboard.id);
      for (const textItem of textItems) {
        const content: string = textItem.parameters?.content || '';
        const fontSize: number = textItem.parameters?.fontSize || 14;
        const color: string = textItem.parameters?.color || '#000000';
        ctx.font = `${fontSize}px sans-serif`;
        ctx.fillStyle = color;
        const tx = textItem.x - artboard.x + 8;
        const ty = textItem.y - artboard.y;
        content.split('\n').forEach((line, li) => {
          ctx.fillText(line, tx, ty + fontSize * 1.4 * (li + 1));
        });
      }
      resolve(canvas.toDataURL('image/png'));
    };

    // 2. Artboard src image
    if (artboard.src) {
      const img = new Image();
      img.onload = () => { ctx.drawImage(img, 0, 0, canvas.width, canvas.height); drawPathsAndText(); };
      img.onerror = () => drawPathsAndText();
      img.src = artboard.src;
    } else {
      drawPathsAndText();
    }
  });
};
```

**위치 2** — SKETCH TO IMAGE runGen 내 sourceItem 처리 (~line 1656)

```typescript
// 변경 전
const sourceItem = selectedItemIds[0] ? canvasItems.find(item => item.id === selectedItemIds[0]) : null;
if (sourceItem) {
  const base64Data = sourceItem.src.split(',')[1];
  const mimeType = sourceItem.src.split(';')[0].split(':')[1];
  parts.unshift({ inlineData: { data: base64Data, mimeType: mimeType } });
}

// 변경 후
const sourceItem = selectedItemIds[0] ? canvasItems.find(item => item.id === selectedItemIds[0]) : null;
if (sourceItem) {
  let compositeSrc: string | null = null;
  if (sourceItem.type === 'artboard') {
    compositeSrc = await createArtboardComposite(sourceItem);
  } else if (sourceItem.src) {
    compositeSrc = sourceItem.src;
  }
  if (compositeSrc) {
    const base64Data = compositeSrc.split(',')[1];
    const mimeType = compositeSrc.split(';')[0].split(':')[1];
    parts.unshift({ inlineData: { data: base64Data, mimeType: mimeType } });
  }
}
```

> `runGen`이 `async` 함수이므로 `await createArtboardComposite()` 직접 사용 가능.

---

### C-9: CHANGE VIEWPOINT GENERATE 버튼 artboard 허용

**위치** — CHANGE VIEWPOINT GENERATE 버튼 disabled 조건 (~line 2835)

```tsx
// 변경 전 (핵심 조건만)
(!canvasItems.find(i => i.id === selectedItemIds[0])?.parameters?.analyzedOpticalParams
  && canvasItems.find(i => i.id === selectedItemIds[0])?.type !== 'generated')

// 변경 후
(!canvasItems.find(i => i.id === selectedItemIds[0])?.parameters?.analyzedOpticalParams
  && canvasItems.find(i => i.id === selectedItemIds[0])?.type !== 'generated'
  && canvasItems.find(i => i.id === selectedItemIds[0])?.type !== 'artboard')
```

---

## 예상 동작

| 상황 | 변경 전 | 변경 후 |
|------|---------|---------|
| 아트보드 생성 | 595×842 (세로) | 960×540 (가로 16:9) |
| 잠금 버튼 클릭 후 재클릭 | unlock 불가 (버튼 클릭 안 됨) | unlock 가능 (컨트롤 바 항상 클릭 가능) |
| 잠금 상태에서 드래그 | CSS pointerEvents:none으로 선택 자체 불가 | 선택은 가능, 드래그/리사이즈만 차단 |
| 아트보드 위 펜 드로잉 후 이동 | 스케치 제자리 | 스케치가 아트보드와 함께 이동 |
| 아트보드 위 텍스트 입력 후 이동 | 텍스트 제자리 | 텍스트가 아트보드와 함께 이동 |
| 아트보드 외부에서 text 시작 | 텍스트 생성 | 차단 (return) |
| SKETCH TO IMAGE — path/text 없이 선택 | 아무 아이템 선택 가능 | artboard/upload/generated 선택 시만 활성화 |
| SKETCH TO IMAGE — artboard 선택 | src만 전송 | 배경+스케치+텍스트 합성 이미지 전송 |
| CHANGE VIEWPOINT — artboard 선택 후 GENERATE | 버튼 비활성화 | 활성화, artboard가 모체(motherId) |

---

## 비고

- C-6 위치 2 (handlePointerMove drag): `startPos`가 있는 모든 아이템을 이동시키므로, 선택된 아이템이 아닌 다른 아트보드의 자식이 의도치 않게 이동하지 않도록 주의. `startPositions`는 선택된 아트보드의 자식만 포함하므로 안전.
- C-8 `createArtboardComposite`는 `handleGenerate` 함수 내부에 선언 (클로저로 `canvasItems` 접근).
- C-9: artboard 선택 시 `analyzedOpticalParams`가 없어도 GENERATE 허용 → handleGenerate 내 V257 인라인 V128 로직이 처리.

# 작업지시서 V276 (최종판)

**작성일**: 2026-03-30
**버전**: V276
**기반 버전**: V272 (적용 완료)
**상태**: 승인 완료

---

## 개요

V272 이후 4개 수정 + 추가요청 1개:

| # | 내용 | 난이도 |
|---|------|--------|
| A | [+] 버튼 재설계 — editVersions 폐기 → artboard 오버레이 스택 | 高 |
| B | VIEWPOINT 다이어그램 오버레이 텍스트 제거 | 低 |
| C | label 위치 — 이미지 하단 외부로 이동 | 低 |
| D | ANALYZE 버튼 폐기 + CHANGE VIEWPOINT 클릭 시 자동 트리거 | 中 |

---

## Q&A 확정 내용

| 질문 | 답변 |
|------|------|
| Q1 | 방안 B — [+] 클릭 시 artboard 새 아이템을 모체 이미지에 오버레이 생성. [<][>]로 스택 탐색 및 스케치 가능 |
| Q2 | 방안 B — "SELECT AN IMAGE AND CLICK ANALYZE" 오버레이만 제거 |
| Q3 | 방안 B — alert 표시 (위치는 Q4로 확인) |
| 추가 | CanvasItem.label 텍스트를 이미지 내부 오버레이가 아닌 이미지 하단 외부에 표시 |

---

## Q&A 미확정

| # | 질문 | 상태 |
|---|------|------|
| Q4 | Q3.B alert 메시지 표시 위치 | 방안 C — 캔버스 중앙 상단 토스트 (3초 자동 소거) |

---

## 변경 상세

---

### Change A: [+] 버튼 재설계 (editVersions → childArtboardIds 스택)

#### A-0: CanvasItem 타입 변경

```ts
// 제거
editVersions?: Array<{ src: string; label: string }>;
activeVersionIndex?: number;

// 추가
childArtboardIds?: string[];   // [+]로 생성된 artboard 자식 ID 목록 (순서 = 생성 순)
activeChildIndex?: number;     // 0 = 부모 자신 표시, 1 = childArtboardIds[0] 표시, ...
```

#### A-1: 제거 항목

- `navigateVersion` 함수 제거
- artboard 렌더링 내 editVersions 기반 displaySrc 제거 (artboard.src 직접 사용)
- 이미지 렌더링 내 editVersions 기반 displaySrc 제거 (item.src 직접 사용)
- G-4 editVersions 탭 네비게이션 UI 제거
- G-5 SKETCH TO IMAGE editVersions 분기 제거 (항상 새 sketch_generated 아이템 생성)
- [+] 버튼 onClick 내 `setSelectedFunction('SKETCH TO IMAGE')`, `setIsRightPanelOpen(true)` 제거
- [+] 버튼 onClick 내 Change E (generated → upload 변환) 제거

#### A-2: navigateStack 함수 추가 (navigateVersion 위치 교체)

```ts
// V276: 스택 탐색 (childArtboardIds 기반)
const navigateStack = (parentId: string, direction: number) => {
  setCanvasItems((prev: CanvasItem[]) => prev.map((i: CanvasItem) => {
    if (i.id !== parentId || !i.childArtboardIds) return i;
    const maxIdx = i.childArtboardIds.length;
    const newIdx = Math.max(0, Math.min(maxIdx, (i.activeChildIndex ?? 0) + direction));
    return { ...i, activeChildIndex: newIdx };
  }));
};
```

#### A-3: [+] 버튼 onClick 교체 (3곳: sketch_generated / generated / upload)

공통 로직:
```ts
onClick={() => {
  // V276: artboard 오버레이 생성 + childArtboardIds에 추가
  const editNum = (item.childArtboardIds?.length ?? 0) + 1;
  const newArtboard: CanvasItem = {
    id: `artboard-edit-${Date.now()}`,
    type: 'artboard',
    src: null,
    x: item.x,
    y: item.y,
    width: item.width,
    height: item.height,
    motherId: item.id,          // path/text 연결용
    label: `EDIT ${String(editNum).padStart(2, '0')}`,
    parameters: {}
  };
  setCanvasItems((prev: CanvasItem[]) => {
    setHistoryStates((prevH: CanvasItem[][]) => [...prevH, prev]);
    const updated = prev.map((i: CanvasItem) =>
      i.id === item.id
        ? {
            ...i,
            childArtboardIds: [...(i.childArtboardIds || []), newArtboard.id],
            activeChildIndex: (i.childArtboardIds?.length ?? 0) + 1
          }
        : i
    );
    return [...updated, newArtboard];
  });
}}
```

#### A-4: 스택 가시성 제어 (렌더링 내 아이템 단위)

아이템 렌더링 최상단 (canvasItems.map 내부):
```ts
// V276: 스택 가시성 — 비활성 멤버 숨김
const stackParent = canvasItems.find((p: CanvasItem) =>
  p.childArtboardIds?.includes(item.id)
);
const isStackHidden = (() => {
  // 이 아이템이 스택 부모인 경우: 자신이 활성 아닌 경우 숨김
  if (item.childArtboardIds?.length && (item.activeChildIndex ?? 0) > 0) return true;
  // 이 아이템이 스택 자식인 경우: 부모의 activeChildIndex가 자신을 가리키지 않으면 숨김
  if (stackParent) {
    const activeChildId = stackParent.childArtboardIds![
      (stackParent.activeChildIndex ?? 1) - 1
    ];
    return activeChildId !== item.id;
  }
  return false;
})();
if (isStackHidden) return null;
```

#### A-5: 스택 네비게이션 UI — 이미지 하단 외부 (G-4 교체)

```tsx
{/* V276: 스택 탐색 UI — childArtboardIds 있는 부모 OR 스택 자식 */}
{(() => {
  const parent = item.childArtboardIds?.length
    ? item
    : canvasItems.find((p: CanvasItem) => p.childArtboardIds?.includes(item.id));
  if (!parent || !parent.childArtboardIds || parent.childArtboardIds.length === 0) return null;
  if (item.type === 'path' || item.type === 'text') return null;
  if (canvasZoom <= 30) return null;

  const activeIdx = parent.activeChildIndex ?? 0;
  const activeLabel = activeIdx === 0
    ? (parent.label || 'ORIGINAL')
    : (() => {
        const childId = parent.childArtboardIds![activeIdx - 1];
        return canvasItems.find((c: CanvasItem) => c.id === childId)?.label || `EDIT ${String(activeIdx).padStart(2, '0')}`;
      })();

  return (
    <div
      className="absolute left-0 right-0 flex items-center justify-between pointer-events-auto z-[25]"
      style={{
        top: '100%',
        paddingTop: `${4 / (canvasZoom / 100)}px`,
      }}
      onPointerDown={(e) => e.stopPropagation()}
    >
      <button
        onClick={() => navigateStack(parent.id, -1)}
        disabled={activeIdx === 0}
        className="font-mono opacity-50 disabled:opacity-20 hover:opacity-100 transition-opacity"
        style={{ fontSize: `${10 / (canvasZoom / 100)}px` }}
      >{'<'}</button>
      <span
        className="font-mono uppercase tracking-widest opacity-60"
        style={{ fontSize: `${10 / (canvasZoom / 100)}px` }}
      >{activeLabel}</span>
      <button
        onClick={() => navigateStack(parent.id, +1)}
        disabled={activeIdx >= parent.childArtboardIds!.length}
        className="font-mono opacity-50 disabled:opacity-20 hover:opacity-100 transition-opacity"
        style={{ fontSize: `${10 / (canvasZoom / 100)}px` }}
      >{'>'}</button>
    </div>
  );
})()}
```

---

### Change B: VIEWPOINT 다이어그램 오버레이 텍스트 제거 (Q2.B)

SitePlanDiagram 내 (~line 242–252) 제거:

```tsx
// 제거 대상
{!sitePlanImage && !isAnalyzing && (
  <div
    className="absolute inset-0 z-20 flex flex-col items-center justify-center rounded-[inherit]"
    style={{ background: 'rgba(255,255,255,0.65)', backdropFilter: 'blur(6px)' }}
  >
    <span className="font-mono text-[10px] uppercase tracking-widest opacity-40 text-center leading-relaxed">
      SELECT AN IMAGE<br />AND CLICK ANALYZE
    </span>
  </div>
)}
```

---

### Change C: label 위치 — 이미지 하단 외부 (추가요청)

F-3 (~line 2789–2802) 수정:

```tsx
// 기존: absolute bottom-0 (이미지 내부 오버레이)
// 변경: absolute top-[100%] (이미지 하단 외부)
{!item.childArtboardIds && item.label && item.type !== 'path' && item.type !== 'text' && canvasZoom > 30 && (
  <div
    className="absolute left-0 right-0 flex items-center justify-center pointer-events-none z-[25]"
    style={{ top: '100%', paddingTop: `${4 / (canvasZoom / 100)}px` }}
  >
    <span
      className="font-mono uppercase tracking-widest opacity-60"
      style={{ fontSize: `${10 / (canvasZoom / 100)}px` }}
    >
      {item.label}
    </span>
  </div>
)}
```

> 아이템 컨테이너에 `overflow: hidden` 없음 → `top: 100%` 클리핑 없이 렌더링 가능 (확인 완료)

---

### Change D: ANALYZE 버튼 폐기 + CHANGE VIEWPOINT 자동 트리거

#### D-1: 함수 선택기 버튼 onClick 수정 (~line 3249)

```tsx
{['PLANNERS', 'SKETCH TO IMAGE', 'CHANGE VIEWPOINT', 'PRINT'].map(fn => (
  <button
    key={fn}
    onClick={() => {
      setSelectedFunction(fn);
      setIsFunctionSelectorOpen(false);
      // V276: CHANGE VIEWPOINT 선택 시 ANALYZE 자동 실행
      if (fn === 'CHANGE VIEWPOINT') {
        const sourceItem = canvasItems.find(
          (i: CanvasItem) => i.id === selectedItemIds[0]
        );
        if (!sourceItem?.src) {
          // [Q4 방안에 따라 메시지 처리]
          return;
        }
        handleAnalyze();
      }
    }}
    ...
  >
```

#### D-2: 패널 하단 버튼 교체 (~line 3347)

```tsx
{/* V276: ANALYZE 버튼 폐기 — 항상 GENERATE 표시 */}
<button
  onClick={isGenerating ? handleCancelGenerate : handleGenerate}
  disabled={isAnalyzing || !analyzedOpticalParams || areSlidersLocked}
  className="relative w-full h-[44px] rounded-full transition-all disabled:opacity-30 disabled:cursor-not-allowed flex items-center justify-center overflow-hidden border border-black/10 dark:border-white/10 enabled:bg-black enabled:text-white enabled:dark:bg-white enabled:dark:text-black"
>
  <span className="font-display tracking-widest uppercase font-medium text-[16px] z-10">
    {isAnalyzing
      ? <Loader2 size={16} className="animate-spin z-10" />
      : isGenerating ? 'CANCEL' : 'GENERATE'}
  </span>
</button>
```

---

## 적용 파일

`src/App.tsx` 단독 수정

---

## 작업 순서 (Q4 확정 후)

1. Change A-0 — CanvasItem 타입 변경 (editVersions/activeVersionIndex 제거, childArtboardIds/activeChildIndex 추가)
2. Change A-1 — 제거 항목 (navigateVersion, G-4, G-5, displaySrc editVersions, Change E)
3. Change A-2 — navigateStack 함수 추가
4. Change A-3 — [+] 버튼 onClick 교체 (3곳)
5. Change A-4 — 스택 가시성 제어 (렌더링 최상단)
6. Change A-5 — 스택 네비게이션 UI (G-4 교체, 이미지 하단 외부)
7. Change B — SitePlanDiagram 오버레이 제거
8. Change C — label top: 100% 이동
9. Change D-1 — 함수 선택기 CHANGE VIEWPOINT onClick 수정
10. Change D-2 — 패널 하단 버튼 교체

# 작업지시서 V290

**작성일**: 2026-03-31
**버전**: V290
**기반 버전**: V289
**상태**: 승인 대기

---

## 개요

| # | 내용 | 난이도 |
|---|------|--------|
| A | 'CHANGE VIEWPOINT' 결과 모체의 라벨 변경 ('GENERATED / VIEW' -> 'IMAGE / VIEW') | 低 |

---

## 변경 상세 계획

### Change A: 'GENERATED / VIEW' 라벨 명칭 변경
`CHANGE VIEWPOINT` 기능 수행 후 원본(모체) 이미지에 부여되는 라벨 및 관련 로직상의 문자열을 모두 수정함.

1.  **라벨 부여 로직 수정** (`src/App.tsx`):
    - `handleAnalyze` 함수 내: `label: 'GENERATED / VIEW'` -> `label: 'IMAGE / VIEW'`
    - `handleGenerate` (CHANGE VIEWPOINT 분기) 내: `label: 'GENERATED / VIEW'` -> `label: 'IMAGE / VIEW'`
2.  **UI 제어 및 데이터 구조 수정** (`src/App.tsx`):
    - 플로팅 툴바 조건문: `item.label === 'GENERATED / VIEW'` -> `item.label === 'IMAGE / VIEW'`
    - 하위 버전(editVersions) 초기화 로직: `label: 'GENERATED / VIEW'` -> `label: 'IMAGE / VIEW'`

---

## 향후 작업 프로세스
1. 승인 발생 시 → `src/App.tsx` 내 해당 문자열 일괄 교체.
2. 완료 후 터미널 빌드 (`npx tsc --noEmit`) 실행.
3. 코드 호스트 서버 업데이트 (`git push`).

# 작업지시서 V285

**작성일**: 2026-03-31
**버전**: V285
**기반 버전**: V284 (적용 완료)
**상태**: 승인 대기

---

## 개요

| # | 내용 | 난이도 |
|---|------|--------|
| A | CHANGE VIEWPOINT 패널 가드 — 이미지 미선택 시 토스트 + 패널 미오픈 | 低 |
| B | SKETCH TO IMAGE 패널 가드 — 아이템 미선택 시 토스트 + 패널 미오픈 | 低 |
| C | 리사이즈 핸들(모서리 원) — 섹션 레벨 오버레이 z-[115]로 격상 | 中 |

---

## Q&A 확정

없음 — 모든 사항 확정

---

## Q&A 미확정

없음

---

## 원인 분석

### A — CHANGE VIEWPOINT 패널 가드

현행 `~line 3525` onClick:

```ts
onClick={() => {
  setSelectedFunction(fn);         // ← 먼저 패널 활성화
  setIsFunctionSelectorOpen(false); // ← 패널 표시 상태로 전환
  if (fn === 'CHANGE VIEWPOINT') {
    if (!sourceItem?.src) {
      setToastMessage('분석할 이미지를 먼저 선택해주세요.'); // 구 문구
      return;                        // 이미 패널이 열린 후 return → 패널이 열린 채로 유지 ✗
    }
    ...
  }
}}
```

수정 방향: 조건 검사를 `setSelectedFunction` 호출 **전**으로 이동. 미선택 시 토스트만 표시하고 return.

---

### B — SKETCH TO IMAGE 패널 가드

현행: SKETCH TO IMAGE 클릭 시 가드 없음 → 선택 여부 무관하게 항상 패널 오픈.

수정 방향: `fn === 'SKETCH TO IMAGE'` 분기 추가 — `selectedItemIds.length === 0` 시 토스트 표시 후 return.

---

### C — 리사이즈 핸들 z-index

현행 DOM 구조:
```
<section> (캔버스 영역)
  <div 통합 오버레이 z-[100]>  ← 스케치/lasso/연결선
  <div 로더 오버레이 z-[102]>  ← V282
  <div selection border z-[105]>  ← V283 A
  <div transform-wrapper (transform → stacking context z=0)>
    <div canvas-item>
      <div resize-handle zIndex:110>  ← 로컬 z, 섹션 z-[100] 이하 ✗
```

transform 속성이 stacking context를 생성하여 내부 zIndex:110은 섹션 레벨 z-[100]보다 아래에 위치.

수정 방향: V283 A (selection border)와 동일한 방식으로 섹션 레벨 리사이즈 핸들 오버레이 추가 (z-[115]).

---

## 변경 상세

---

### Change A: CHANGE VIEWPOINT 패널 가드

`~line 3525` (함수 선택 버튼 onClick):

```ts
// 변경 전
onClick={() => {
  setSelectedFunction(fn);
  setIsFunctionSelectorOpen(false);
  if (fn === 'CHANGE VIEWPOINT') {
    const sourceItem = canvasItems.find((i: CanvasItem) => i.id === selectedItemIds[0]);
    if (!sourceItem?.src) {
      setToastMessage('분석할 이미지를 먼저 선택해주세요.');
      setTimeout(() => setToastMessage(''), 3000);
      return;
    }
    ...
  }
}}

// 변경 후
onClick={() => {
  // V285 A: CHANGE VIEWPOINT — 미선택 가드 (setSelectedFunction 전에 검사)
  if (fn === 'CHANGE VIEWPOINT') {
    const sourceItem = canvasItems.find((i: CanvasItem) => i.id === selectedItemIds[0]);
    if (!sourceItem?.src) {
      setToastMessage('이미지를 먼저 선택해주세요.');
      setTimeout(() => setToastMessage(''), 3000);
      return;  // 패널 미오픈
    }
    // 이미 분석된 경우 상태 복원
    if (sourceItem.parameters?.analyzedOpticalParams) {
      setAnalyzedOpticalParams(sourceItem.parameters.analyzedOpticalParams);
      setElevationParams(sourceItem.parameters.elevationParams || null);
      setSitePlanImage(sourceItem.parameters.sitePlanImage || null);
      setElevationImages(sourceItem.parameters.elevationImages || null);
      setCvPrompt(sourceItem.parameters.cvPrompt || '');
      setSelectedFunction(fn);
      setIsFunctionSelectorOpen(false);
      return;
    }
    setSelectedFunction(fn);
    setIsFunctionSelectorOpen(false);
    handleAnalyze();
    return;
  }
  setSelectedFunction(fn);
  setIsFunctionSelectorOpen(false);
}}
```

---

### Change B: SKETCH TO IMAGE 패널 가드

`~line 3525` (Change A와 동일 위치, SKETCH TO IMAGE 분기 추가):

```ts
// Change A 적용 코드의 fn 분기 앞에 추가
// V285 B: SKETCH TO IMAGE — 미선택 가드
if (fn === 'SKETCH TO IMAGE') {
  if (selectedItemIds.length === 0) {
    setToastMessage('스케치를 먼저 선택해주세요.');
    setTimeout(() => setToastMessage(''), 3000);
    return;  // 패널 미오픈
  }
}
```

---

### Change C: 리사이즈 핸들 섹션 레벨 오버레이 (z-[115])

V283 A selection border 오버레이 바로 뒤에 삽입.

**선택 아이템 조건** (기존 핸들과 동일):
- `item.type !== 'path' && item.type !== 'text'`
- `!item.isLocked`
- 선택된 단일 아이템 (`selectedItemIds.length === 1`) — 다중 선택 시 핸들 없음

**C-1: 섹션 레벨 리사이즈 핸들 오버레이 삽입** (selection border 오버레이 닫힘 태그 뒤):

```tsx
{/* V285 C: 섹션 레벨 리사이즈 핸들 오버레이 (z-115) — 모든 오버레이 위 */}
{selectedItemIds.length === 1 && (() => {
  const resItem = canvasItems.find((i: CanvasItem) => i.id === selectedItemIds[0]);
  if (!resItem || resItem.type === 'path' || resItem.type === 'text' || resItem.isLocked) return null;
  const s = 1 / (canvasZoom / 100);
  const size = 12 * s;
  return (
    <div
      className="absolute inset-0 pointer-events-none z-[115]"
      style={{
        transform: `translate(calc(50% + ${canvasOffset.x}px), calc(50% + ${canvasOffset.y}px)) scale(${canvasZoom / 100})`,
        transformOrigin: '0 0'
      }}
    >
      {[
        { dx: -1, dy: -1, cursor: 'nwse-resize', x: resItem.x - size / 2, y: resItem.y - size / 2 },
        { dx:  1, dy: -1, cursor: 'nesw-resize', x: resItem.x + resItem.width - size / 2, y: resItem.y - size / 2 },
        { dx: -1, dy:  1, cursor: 'nesw-resize', x: resItem.x - size / 2, y: resItem.y + resItem.height - size / 2 },
        { dx:  1, dy:  1, cursor: 'nwse-resize', x: resItem.x + resItem.width - size / 2, y: resItem.y + resItem.height - size / 2 },
      ].map((pos, idx) => (
        <div
          key={`rh-${idx}`}
          className="resize-handle"
          data-dx={pos.dx}
          data-dy={pos.dy}
          style={{
            position: 'absolute',
            left: pos.x,
            top: pos.y,
            width: size,
            height: size,
            borderWidth: 1.6 * s,
            borderStyle: 'solid',
            backgroundColor: 'white',
            borderColor: '#808080',
            borderRadius: '999px',
            pointerEvents: 'auto',
            cursor: pos.cursor,
          }}
        />
      ))}
    </div>
  );
})()}
```

**C-2: 기존 canvas-item 내부 핸들 제거**

`~line 3444` (canvas-item 내부 Corner Handles 블록 전체 제거):

```tsx
// 제거 대상:
{/* Corner Handles (Scale Invariant Circles, 4-corner resizable) */}
{[
  { top: true, left: true, dx: -1, dy: -1, cursor: 'nwse-resize' },
  ...
].map(...)}
```

> 섹션 레벨 오버레이로 완전 대체하므로 canvas-item 내부 핸들 불필요.

---

## 적용 파일

`src/App.tsx` 단독 수정

---

## 작업 순서

1. Change A — CHANGE VIEWPOINT 가드 재구성 (조건 순서 변경 + 문구 수정)
2. Change B — SKETCH TO IMAGE 가드 추가
3. Change C-1 — 섹션 레벨 리사이즈 핸들 오버레이 삽입
4. Change C-2 — 기존 canvas-item 내부 핸들 제거
5. `npx tsc --noEmit` 검증

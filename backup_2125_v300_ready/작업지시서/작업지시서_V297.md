# 작업지시서 V297 (최종 통합본)

**작성일**: 2026-03-31
**버전**: V297 — V293 + V295 + V296 전항목 통합
**상태**: 승인 완료 — 전항목 확정

---

## 개요

| # | 출처 | 내용 | 위치 | 확정 |
|---|------|------|------|------|
| A | V293 | AEPL v4 키 통일 — `layerC_property` 구버전 키 교체 | ~line 1787 | ✅ |
| B | V293 | `getSemanticAngle` case 포맷 수정 — `'03:00'`→`'3:00'`, `'01:30'`→`'1:30'` | ~line 1746 | ✅ |
| C | V293 | 라이브러리 패널 입면도 참조 — `item.parameters` 직접 참조, fallback null | ~line 3514 | ✅ |
| D | V293 | Phase 0 교체 — 이미지 재생성 → 텍스트 묘사 호출 (영어+환경 포함) | ~line 1380 | ✅ |
| E | V296 | Phase 1 JSON 스키마에 `front_facade_evidence` 필드 추가 (Q4) | ~line 1449 | ✅ |
| F | V296 | Phase 2 `buildingDescription` + `config temperature:0.1` 주입 (Q1+Q2) | ~line 1555 | ✅ |
| G | V296 | Phase 4 MICRO-DESCRIPTION 블록 + 권한 분리 + `config temperature:0.1` + De-bake (Q1+Q2+Q3) | ~line 1786 | ✅ |
| H | V295 | `generateElevations` Cross 합성 + ELEVATION SHEET 캔버스 아이템 자동 생성 | ~line 1624 | ✅ |
| I | V295 | `upload` 제어바 LIBRARY 버튼 및 구분선 삭제 | ~line 3176 | ✅ |

---

## 변경 상세

---

### Change A — AEPL v4 키 통일

**위치**: `~line 1787–1791`

```ts
// 변경 전
- Mass Typology: ${localElevationParams['1_macro_geometry']?.mass_typology || 'N/A'}
- Roof Form: ${localElevationParams['1_macro_geometry']?.roof_form || 'N/A'}
- Base Material: ${localElevationParams['3_material']?.base_material_type || 'N/A'}
- Fenestration: ${localElevationParams['4_fenestration']?.fenestration_type || 'N/A'}

// 변경 후
// V297 A: AEPL v4 키 통일
- Mass Typology: ${localElevationParams['1_Geometry_MASTER']?.mass_typology || 'N/A'}
- Roof Form: ${localElevationParams['1_Geometry_MASTER']?.roof_form || 'N/A'}
- Base Material: ${localElevationParams['2_Property_SLAVE']?.base_material_type || 'N/A'}
- Fenestration: ${localElevationParams['2_Property_SLAVE']?.fenestration_type || 'N/A'}
```

---

### Change B — `getSemanticAngle` case 수정

**위치**: `~line 1747–1748`

```ts
// 변경 전
case '03:00': return '03:00 (Direct Right Side / Profile View)';
case '01:30': return '01:30 (Rear-Right Corner View / 2-Point Perspective)';

// 변경 후
// V297 B: ANGLES 상수 형식 일치 ('3:00', '1:30')
case '3:00': return '3:00 (Direct Right Side / Profile View)';
case '1:30': return '1:30 (Rear-Right Corner View / 2-Point Perspective)';
```

---

### Change C — 라이브러리 패널 입면도 참조 수정

**위치**: `~line 3514`

```ts
// 변경 전
const imgSrc = elevationImages[currentView.key as keyof typeof elevationImages];

// 변경 후
// V297 C: item.parameters 직접 참조, fallback null (component state 오염 차단)
const elevImgs = item.parameters?.elevationImages ?? null;
const imgSrc = elevImgs?.[currentView.key as keyof typeof elevImgs] ?? null;
```

---

### Change D — Phase 0 교체 (텍스트 묘사 호출)

#### D-1: `analyzeViewpoint` 시그니처 수정

**위치**: `~line 1415`

```ts
// 변경 전
const analyzeViewpoint = async (base64Image: string, itemId?: string) => {

// 변경 후
// V297 D: buildingDescription 파라미터 추가
const analyzeViewpoint = async (base64Image: string, itemId?: string, buildingDescription: string = '') => {
```

#### D-2: `analyzeViewpoint` 내부 context prefix 추가

**위치**: `~line 1422` (analysisPrompt 조립 직전)

```ts
// V297 D: 묘사 텍스트 context prefix 주입
const descriptionContext = buildingDescription
  ? `[PRE-ANALYSIS DESCRIPTION]\n${buildingDescription}\n\n`
  : '';

const analysisPrompt = `${descriptionContext}[Protocol A: Architectural Logic Engine...]
  ...
`;
```

#### D-3: `handleAnalyze` Phase 0 블록 교체

**위치**: `~line 1381–1406` (이미지 재생성 블록 전체 교체)

```ts
// 변경 전 (이미지 재생성 API 호출 전체 블록)
const regenResult = await ai.models.generateContent({
  model: ANALYSIS,
  contents: { parts: [{ inlineData: ... }, { text: 'Reproduce this architectural image...' }] }
});
// → 재생성 이미지 추출 후 analysisImage 교체

// 변경 후
// V297 D: Phase 0 — 이미지 재생성 → 텍스트 묘사 호출 (원본 이미지 불변)
let buildingDescription = '';
try {
  const descResult = await ai.models.generateContent({
    model: ANALYSIS,
    contents: {
      parts: [
        { inlineData: { data: base64Data, mimeType } },
        { text: 'Describe this architectural image in detail. Cover building form, massing, materials, fenestration, roof type, proportions, surrounding environment, sky, landscaping, weather, and any distinctive features. Be precise and comprehensive. Respond in English only.' }
      ]
    }
  });
  buildingDescription = descResult.candidates?.[0]?.content?.parts?.[0]?.text || '';
} catch (descErr) {
  console.warn('[V297 D] Phase 0 묘사 호출 실패, 묘사 없이 진행', descErr);
}
// analysisImage = sourceItem.src 그대로 유지 (교체 없음)
```

#### D-4: `handleAnalyze` 호출부 수정

**위치**: `~line 1412`

```ts
// 변경 전
await analyzeViewpoint(analysisImage, sourceItem.id);

// 변경 후
// V297 D: 원본 이미지 + 묘사 텍스트 전달
await analyzeViewpoint(sourceItem.src, sourceItem.id, buildingDescription);
```

#### D-5: `handleAnalyze` — `buildingDescription` parameters 저장

**위치**: `~line 1511–1520` (newParams 객체)

```ts
// 변경 후 (newParams에 추가)
const newParams = {
  angleIndex: ...,
  ...
  buildingDescription,   // V297 D-5: Phase 2/4에서 참조
  sitePlanImage: null,
  architecturalSheetImage: null
};
```

#### D-6: `handleGenerate` inline 경로 수정 + 공통 함수 추출

**위치**: `~line 1722`

```ts
// 공통 함수 (handleAnalyze 위에 위치)
// V297 D-6: Phase 0 묘사 공통 함수
const getArchitecturalDescription = async (src: string): Promise<string> => {
  try {
    const b64 = src.split(',')[1];
    const mime = src.split(';')[0].split(':')[1];
    const descResult = await ai.models.generateContent({
      model: ANALYSIS,
      contents: {
        parts: [
          { inlineData: { data: b64, mimeType: mime } },
          { text: 'Describe this architectural image in detail. Cover building form, massing, materials, fenestration, roof type, proportions, surrounding environment, sky, landscaping, weather, and any distinctive features. Be precise and comprehensive. Respond in English only.' }
        ]
      }
    });
    return descResult.candidates?.[0]?.content?.parts?.[0]?.text || '';
  } catch { return ''; }
};

// handleGenerate inline 경로 (~line 1722)
// 변경 전
const analysisResult = await analyzeViewpoint(activeSrc, sourceItem.id);

// 변경 후
// V297 D-6: inline 경로도 묘사 선행 호출
const inlineDesc = await getArchitecturalDescription(activeSrc);
const analysisResult = await analyzeViewpoint(activeSrc, sourceItem.id, inlineDesc);
```

---

### Change E — Phase 1 JSON 스키마 `front_facade_evidence` 추가

**위치**: `~line 1449–1458` (JSON 스키마 return 블록)

```ts
// 변경 전
{
  "angle": "One of: 12:00, 1:30, 3:00, 04:30, 06:00, 07:30, 09:00, 10:30",
  "altitude_index": "0 to 7",
  ...
}

// 변경 후
// V297 E: front_facade_evidence 필드 최상단 추가 (Chain-of-Thought — UI 미출력)
{
  "front_facade_evidence": "Explain exactly which side is the FRONT (06:00) based on main entrance, large windows, and primary facade evidence, BEFORE determining angle.",
  "angle": "One of: 12:00, 1:30, 3:00, 04:30, 06:00, 07:30, 09:00, 10:30",
  "altitude_index": "0 to 7",
  "lens_index": "0 to 7",
  "time_index": "0 to 7",
  "elevation_parameters": {
    "1_Geometry_MASTER": { ... },
    "2_Property_SLAVE": { ... }
  }
}
```

> `front_facade_evidence`는 파싱 후 자동 무시 — UI 미출력, 별도 파싱 코드 수정 불필요

---

### Change F — Phase 2 `buildingDescription` + `temperature:0.1` 주입

#### F-1: `generateElevations` 시그니처 수정

**위치**: `~line 1555`

```ts
// 변경 전
const generateElevations = async (base64Image: string, extractedParams?: any, itemId?: string) => {

// 변경 후
// V297 F: buildingDescription 파라미터 추가
const generateElevations = async (base64Image: string, extractedParams?: any, itemId?: string, buildingDescription: string = '') => {
```

#### F-2: `analyzeViewpoint` 내부 `generateElevations` 호출부 수정

**위치**: `~line 1527`

```ts
// 변경 전
const elImages = await generateElevations(base64Image, data.elevation_parameters, itemId);

// 변경 후
// V297 F: buildingDescription 전달
const elImages = await generateElevations(base64Image, data.elevation_parameters, itemId, buildingDescription);
```

#### F-3: `generateOne` 프롬프트에 `buildingDescription` 주입

**위치**: `~line 1576–1591` (generateOne 내부 prompt)

```ts
// 변경 후 (PHASE 1 블록 위에 추가)
// V297 F: buildingDescription 블록 추가
const descBlock = buildingDescription
  ? `\n[SOURCE IMAGE DESCRIPTION]\n${buildingDescription}\n`
  : '';

const prompt = `
  [Protocol B: Visualization Execution Engine v2.2]

  TASK: Deterministic Rendering of the building's ${config.label} Orthographic View.
  CONTEXT: [ensemble_pair AEPS-v4] ${contextualParamsStr}
  ${descBlock}
  [PHASE 1: GEOMETRY MASTER LOCKING]
  ...
`.trim();
```

#### F-4: Phase 2 API 호출에 `config` 추가

**위치**: `~line 1593–1601` (generateOne 내부 `ai.models.generateContent`)

```ts
// 변경 전
const result = await ai.models.generateContent({
  model: IMAGE_GEN,
  contents: { parts: [...] },
});

// 변경 후
// V297 F: temperature 0.1 강제 (config 키 사용 — generationConfig 아님)
const result = await ai.models.generateContent({
  model: IMAGE_GEN,
  contents: { parts: [...] },
  config: { temperature: 0.1, topK: 1 }
});
```

---

### Change G — Phase 4 권한 분리 + MICRO-DESCRIPTION + De-bake + `temperature:0.1`

#### G-1: `handleGenerate`에서 `buildingDescription` 로드

**위치**: `~line 1716–1731` (localElevationParams 로드 블록 하단)

```ts
// V297 G: buildingDescription 로드 (item.parameters에서)
const localBuildingDescription: string = sourceItem.parameters?.buildingDescription || '';
```

#### G-2: `layerC_property` 내 MICRO-DESCRIPTION 블록 추가

**위치**: `~line 1786–1791` (layerC_property 변수 하단)

```ts
// 변경 후 (layerC_property 뒤에 추가)
// V297 G: MICRO-DESCRIPTION 블록 (Phase 0 묘사 → Phase 4 주입)
const layerC_microDesc = localBuildingDescription
  ? `\n## Step 6: Original Image Micro-Description (Immutable Reference)\n[ORIGINAL IMAGE MICRO-DESCRIPTION]\n${localBuildingDescription}`
  : '';
```

#### G-3: `layerC_blindspot` De-bake 텍스트 교체

**위치**: `~line 1793–1795`

```ts
// 변경 전
const layerC_blindspot = `## Step 3: Layering & Blind Spot Inference (Void Mitigation)
- Context Void Mitigation: Synchronize Foreground/Background.
- Material Injection: Lock original textures. Relighting only for solar angle (${currentTime}).`;

// 변경 후
// V297 G: De-bake 그림자 분리 + 태양광 재계산 강령 추가
const layerC_blindspot = `## Step 3: Layering & Blind Spot Inference (Void Mitigation)
- Context Void Mitigation: Synchronize Foreground/Background.
- Material Injection: Lock original textures. Relighting only for solar angle (${currentTime}).
- Shadow De-bake: Dark regions in source = CAST SHADOWS (not paint/texture).
  Do NOT copy shadow patterns to new surfaces.
  Re-cast shadows from sun position at ${currentTime}.`;
```

#### G-4: Phase 4 권한 분리 텍스트 추가 및 `layerC_microDesc` 삽입

**위치**: `~line 1801–1807` (finalPrompt 구성)

```ts
// 변경 전
const finalPrompt = `
# SYSTEM: 5-Point Integrated Viewpoint Simulation Architect (5-IVSP)
# GOAL: Change the angle of view of the provided architectural image.
- Geometric Sanctuary: The building's proportions are Immutable Constants.
# INPUT IMAGES: IMAGE 1 (Primary) + IMAGE 2 (Geometric Reference: ${elevationLabel})
${layerB_viewpoint}\n${layerC_blindspot}\n${layerC_property}${cvPrompt...}
[GENERATE IMAGE NOW]`.trim();

// 변경 후
// V297 G: 권한 분리 룰 + MICRO-DESCRIPTION 블록 추가
const finalPrompt = `
# SYSTEM: 5-Point Integrated Viewpoint Simulation Architect (5-IVSP)
# GOAL: Change the angle of view of the provided architectural image.
- Geometric Sanctuary: The building's proportions are Immutable Constants.
# INPUT IMAGES: IMAGE 1 (Primary) + IMAGE 2 (Geometric Reference: ${elevationLabel})
# IMAGE AUTHORITY RULE:
  - IMAGE 1 (Original): ABSOLUTE TRUTH for materials, textures, colors, and style.
  - IMAGE 2 (Elevation): GEOMETRY GUIDE ONLY — silhouette and proportions reference, NOT color/material source.
${layerB_viewpoint}\n${layerC_blindspot}\n${layerC_property}${layerC_microDesc}${cvPrompt.trim() ? `\n\n## Additional Creative Direction\n${cvPrompt.trim()}` : ''}
[GENERATE IMAGE NOW]`.trim();
```

#### G-5: Phase 4 API 호출에 `config` 추가

**위치**: `~line 1818` (`runGen` 내부 `ai.models.generateContent`)

```ts
// 변경 전
const response = await ai.models.generateContent({ model: modelName, contents: { parts } });

// 변경 후
// V297 G: temperature 0.1 강제
const response = await ai.models.generateContent({
  model: modelName,
  contents: { parts },
  config: { temperature: 0.1, topK: 1 }
});
```

---

### Change H — Cross 합성 + ELEVATION SHEET 자동 생성

**위치**: `~line 1624–1642` (`setElevationImages` 이후) — 기존 `setCanvasItems` 블록 교체

```ts
// V297 H: Cross 합성 + ELEVATION SHEET 캔버스 아이템 자동 생성
const buildCrossComposite = (imgs: typeof newImages): Promise<string | null> =>
  new Promise((resolve) => {
    const entries = [
      { key: 'rear',  col: 1, row: 0 },
      { key: 'left',  col: 0, row: 1 },
      { key: 'top',   col: 1, row: 1 },
      { key: 'right', col: 2, row: 1 },
      { key: 'front', col: 1, row: 2 },
    ];
    const cellSize = 512;
    const canvas = document.createElement('canvas');
    canvas.width = cellSize * 3;
    canvas.height = cellSize * 3;
    const ctx = canvas.getContext('2d');
    if (!ctx) { resolve(null); return; }
    ctx.fillStyle = '#121212';
    ctx.fillRect(0, 0, canvas.width, canvas.height);
    let loaded = 0;
    for (const { key, col, row } of entries) {
      const src = (imgs as any)[key];
      if (!src) { if (++loaded === 5) resolve(canvas.toDataURL('image/png')); continue; }
      const img = new Image();
      img.onload = () => {
        ctx.drawImage(img, col * cellSize, row * cellSize, cellSize, cellSize);
        if (++loaded === 5) resolve(canvas.toDataURL('image/png'));
      };
      img.onerror = () => { if (++loaded === 5) resolve(canvas.toDataURL('image/png')); };
      img.src = src;
    }
  });

const sheetImage = await buildCrossComposite(newImages);

if (itemId) {
  setCanvasItems(prev => {
    const sourceItem = prev.find(i => i.id === itemId);

    // ① 소스 아이템 parameters 업데이트
    const updatedItems = prev.map(item => {
      if (item.id === itemId && item.parameters) {
        return {
          ...item,
          parameters: {
            ...item.parameters,
            elevationImages: newImages,
            sitePlanImage: newImages.top,
            architecturalSheetImage: sheetImage ?? null
          }
        };
      }
      return item;
    });

    if (!sourceItem || !sheetImage) return updatedItems;

    // ② ELEVATION SHEET 캔버스 아이템 자동 생성
    const sheetItem: CanvasItem = {
      id: `elevation-sheet-${Date.now()}`,
      type: 'sketch_generated',
      src: sheetImage,
      x: sourceItem.x,
      y: sourceItem.y + sourceItem.height + 100,
      width: sourceItem.width,
      height: sourceItem.width,
      motherId: itemId,
      label: 'ELEVATION SHEET',
      parameters: {
        architecturalSheetImage: sheetImage,
        angleIndex: 0, altitudeIndex: 0, lensIndex: 0, timeIndex: 0
      }
    };

    return [...updatedItems, sheetItem];
  });
}
```

> 기존 `setCanvasItems(prev => prev.map(...))` 블록 (~line 1628–1642) 전체 교체

---

### Change I — `upload` 제어바 LIBRARY 버튼 삭제

**위치**: `~line 3176–3183`

```tsx
// 변경 전 (전체 블록 삭제)
<div className="w-[1px] bg-black/10 dark:bg-white/10 mx-0.5" style={{ height: (28 / (canvasZoom / 100)) + 'px' }} />
<button
  onClick={() => setOpenLibraryItemId(openLibraryItemId === item.id ? null : item.id)}
  className="flex items-center justify-center hover:bg-black/5 dark:hover:bg-white/5 transition-colors rounded-full"
  style={{ width: `${36 / (canvasZoom / 100)}px`, height: `${36 / (canvasZoom / 100)}px` }}
  title="입면 전개도"
>
  <Book size={12 / (canvasZoom / 100)} />
</button>

// 변경 후
// V297 I: LIBRARY 버튼 및 구분선 삭제 (ELEVATION SHEET는 Change H에 의해 자동 생성됨)
// → 위 블록 전체 제거
```

---

## 적용 파일

`src/App.tsx` 단독 수정

---

## 작업 순서

1. **Change A** — `layerC_property` 키 4개 교체 (~line 1787)
2. **Change B** — `getSemanticAngle` case 2개 수정 (~line 1747)
3. **Change C** — 라이브러리 입면도 참조 수정 (~line 3514)
4. **Change D** — `analyzeViewpoint` 시그니처 + context prefix → Phase 0 블록 교체 → 호출부 수정 → `getArchitecturalDescription` 추출 → inline 경로 수정 → `buildingDescription` parameters 저장
5. **Change E** — Phase 1 JSON 스키마 `front_facade_evidence` 추가 (~line 1449)
6. **Change F** — `generateElevations` 시그니처 + 호출부 + 프롬프트 descBlock + API config 추가 (~line 1555)
7. **Change G** — `buildingDescription` 로드 → `layerC_microDesc` 추가 → `layerC_blindspot` De-bake 교체 → finalPrompt 권한 분리 + microDesc 삽입 → Phase 4 API config 추가 (~line 1716)
8. **Change H** — `buildCrossComposite` + ELEVATION SHEET 아이템 생성 로직 (기존 setCanvasItems 교체) (~line 1624)
9. **Change I** — `upload` 제어바 LIBRARY 버튼+구분선 삭제 (~line 3176)
10. `npx tsc --noEmit` 검증
